# 2026-07-17 코딩 이론·모의고사 개편·진도 대시보드 (2차 확충)

## 배경
- 1차 확충(실기 이론·코드 예측·시험 정보) 후 대표 지시로 격차 보완 3종 진행.
- 원칙 동일: 참고 사이트(정처기 감자)는 구조·기능 명세만 참고, 콘텐츠는 전부 자체 작성.
- 대표가 붙여준 페이지 소스들에서 문제 본문은 얻을 수 없음(로그인 후 클라이언트 로딩 구조) —
  구조 정보와 공단 공개 통계(합격률)만 추출해 활용.

## 추가된 것

### 1. 코딩 이론 5과목 (48챕터)
- `src/data/coding-theory/` — common-sql(공통 8·SQL 8) + c-java(C 10·Java 10) + python(12) + index
- Python 예시 출력값은 python3 실행 검증, 각 챕터 끝 "출제 포인트" 표
- 페이지: `CodingTheoryHome`(과목 카드), `CodingTheoryStudy`(챕터 학습 + 완료 체크)
- 라우트: `/practical-exam/:certType/coding-theory(/:areaId)`
- 메뉴: 3개 자격증 실기시험 섹션에 '코딩 이론' 추가

### 2. 실기 모의시험 출제 개편
- `getPracticalRoundQuestions`: 코드 결과 6 + SQL 5 + 알고리즘 4 + 단답 5 = 20문항 (실제 실기 비중 재현)
- 코드 결과 예측 57문제를 공용 풀(codePool)로 편입
- PracticalMockTest·PracticalTestResult에 code 타입 렌더링(<pre>)·배지·유형별 점수 추가

### 3. 학습 진도
- `src/hooks/useStudyProgress.ts` — sb_study_progress 기반, key = `areaId/chapterId`
  - 주의: 테이블 PK가 serial이라 upsert 중복 위험 → 클라이언트에서 이미 완료된 챕터는 재저장 안 함
- 실기 이론·코딩 이론 학습 페이지에 "이 챕터 학습 완료" 버튼 + 사이드바 체크
- 마이페이지에 '학습 진도' 탭 — 영역별 진도율 바(실기 이론 + 코딩 이론)

### 4. 시험 정보 — 합격률 실측 표
- 2020~2026 회차별 실기 합격률 표 반영 (공단 공개 통계, 대표 승인)
- 2026년 1회: 응시 23,898명 / 합격 4,335명 (18.1%)

## 진행 중 이력 (인수인계용)
- 대표가 코딩 이론 연결을 한 차례 "되돌리기"로 답했다가 곧바로 "모두 수락"으로 번복 → 최종 전부 반영.
- 향후 기능 후보(참고 사이트 로그인 메뉴에서 확인): 오답노트, 학습 일정, 배지, 필기 노트.

## 검증
- `tsc -b` 에러 0, `vite build` 성공

## 남은 일
- 대표 본인 저작 교재 수령 시 콘텐츠 전면 검수·보강
- 오답노트 등 후보 기능 도입 여부 결정
