# 2026-07-17 개발 환경 준비 (재개)

## 한 일
1. **로컬 사본 복원**: 데이터 드라이브에 eip 로컬 사본이 없어 GitHub(aebonlee/eip)에서
   `/Volumes/aebon - 데이터/dreamit-web/01-edu-sites/eip`로 클론.
2. **의존성 설치**: `npm install` 완료 (React 19 / Vite 8 / TailwindCSS 4 / Supabase).
3. **Supabase fallback 하드코딩** (`src/lib/supabase.ts`):
   - 기존: placeholder URL/key fallback → 클린 빌드에서 env 누락 시 로그인 통째로 깨지는 구조.
   - 수정: 공용 프로젝트(hcmgdztsgjvzcyxyayaj) URL/anon key를 fallback으로 하드코딩 (전역 규칙).
   - `isSupabaseConfigured`는 fallback 존재로 항상 true.
4. **빌드 검증**: `tsc -b` 에러 0, `vite build` 성공.

## 현재 상태 확인
- 배포: gh-pages 브랜치, **legacy build_type**, CNAME eip.dreamitbiz.com (루트+public 양쪽 유지), 상태 built.
- vite base 미지정(기본 `/`) — 커스텀 도메인 규칙 부합.
- 마지막 개발: 2026-05-14 (page-header 위치 + 다크모드 토글 가시성 수정).
- DB: `sb_` 접두사 테이블 (sb_profiles, sb_study_progress, sb_test_results, sb_code_submissions).
  - 주의: 접두어가 사이트id(eip_)가 아닌 `sb_`로 되어 있음 — 기존 데이터가 있으므로 임의 변경 금지.

## 사고/주의
- 번들 1MB(gzip 302KB) 청크 경고 — 동작에는 문제 없음, 추후 code-split 검토 대상.
- gh-pages 수동 배포는 2~3분 간격 유지 (레이트리밋).

## 남은 일
- 다음 개발 요구사항 수령 대기 (콘텐츠 확충/기능 추가 방향 미정).
