# 2026-07-17 실기 이론·코드 결과 예측·시험 정보 확충

## 배경
- 대표 지시: 정처기 감자(jeongcheogi.edugamja.com)를 참고해 콘텐츠 확충.
- 원칙: 타사 유료 콘텐츠 복제는 저작권 리스크로 불가 →
  **커리큘럼 구조·주제 범위만 동일하게 커버하고 본문·문제는 전부 자체 작성** (출제기준·표준 교과 지식 기반).

## 추가된 것

### 1. 실기 이론 5영역 (48챕터)
- `src/data/practical-theory/` — types + sw(설계 8·개발 8) + db-network(DB 12·네트워크OS 10) + security(10) + index
- 페이지: `PracticalTheoryHome`(영역 카드), `PracticalTheoryStudy`(챕터 학습, 공용 `MarkdownContent` 렌더러)
- 라우트: `/practical-exam/:certType/theory`, `/practical-exam/:certType/theory/:areaId`
- 계산 유형(서브넷, 스케줄링, 페이지 교체, chmod)은 단계별 풀이 포함

### 2. 코드 결과 예측 (57문제)
- `src/data/code-reading/` — types + c-java(15+15) + python-sql(15+12) + index
- **전 문제 실제 실행 검증**: C/Java는 컴파일·실행, Python은 python3 실행으로 answer 일치 확인
- 페이지: `CodeReadingPractice`(언어 탭 + 단답 채점 + 해설)
- 라우트: `/practical-exam/:certType/code-reading/:language`

### 3. 시험 정보 페이지
- `src/pages/exam-info/ExamInfoPage.tsx` — 원서접수 절차 / 응시자격 / 합격기준·합격률 경향 / 시험장 안내 4탭
- 합격률 수치는 공식 자료 미수령으로 경향 서술 + 큐넷 링크만 (수치 확정 시 갱신)
- 라우트: `/exam-info`

### 4. 메뉴·홈 통합
- Header 3개 자격증 실기시험 섹션에 '실기 이론'·'코드 결과 예측' 추가
- PracticalExamHome에 카드 2종 추가
- 상단 '시험 정보' 메뉴는 대표 승인 후 추가 완료 (커뮤니티 좌측)

### 5. 공용 컴포넌트
- `src/components/MarkdownContent.tsx` — ChapterStudy 렌더러와 동일 규칙 추출 (##/###/리스트/표/강조)

## 검증
- `tsc -b` 에러 0, `vite build` 성공 (기존 1MB 청크 경고만)

## 남은 일
- 합격률 공식 수치·2026 시험일정 자료 수령 시 반영
- 실기 강의 영상 링크 수령 시 강의실 추가
- 유료화(이용권) 도입 여부 결정
