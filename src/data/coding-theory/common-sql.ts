import type { TheoryArea } from '../practical-theory/types'

// 정보처리기사 실기 대비 — 코딩 이론(공통 기초 + SQL)
// 자체 작성 원본 콘텐츠. 마크다운 부분집합(##, ###, -, 표, **강조**, 일반 문단)만 사용.

export const commonCodingArea: TheoryArea = {
  id: 'common',
  name: '공통 기초',
  icon: 'fa-solid fa-cubes',
  description: 'C·Java·Python 어떤 언어로 출제되어도 통하는 프로그래밍 공통 개념. 실기 코드 문제를 읽고 결과를 추적하는 기초 체력을 기른다.',
  chapters: [
    {
      id: 'common-reading-order',
      name: '코드를 읽는 순서',
      topics: ['위에서 아래로', '함수 호출과 점프', '실기 문제 읽는 요령'],
      content: `## 코드를 읽는 순서

프로그램은 기본적으로 **위에서 아래로(Top-Down)** 한 줄씩 실행됩니다. 요리 레시피와 똑같습니다. 1번 단계를 건너뛰고 3번을 먼저 하면 요리가 망가지듯, 컴퓨터도 적힌 순서대로만 일합니다. 이 원칙을 **순차 실행(Sequential Execution)** 이라고 합니다.

### 실행 순서가 바뀌는 세 가지 경우

- **선택(조건문)**: 갈림길. 조건이 참이면 이쪽, 거짓이면 저쪽 길로 갑니다.
- **반복(반복문)**: 회전교차로. 조건이 만족되는 동안 같은 구간을 빙빙 돕니다.
- **함수 호출**: 순간이동. 호출한 지점에서 함수 본문으로 점프했다가, 끝나면 **호출한 다음 위치로 되돌아옵니다**.

### 함수 호출 시 점프 규칙

함수는 "전화 걸기"에 비유할 수 있습니다. 통화(함수 실행)가 끝나면 반드시 전화를 건 사람(호출부)에게 돌아옵니다.

- 프로그램의 출발점은 **main 함수**(C·Java) 또는 파일의 첫 줄(Python)입니다.
- 함수 정의가 코드 위쪽에 적혀 있어도, **호출되기 전까지는 실행되지 않습니다**.
- 함수가 값을 돌려주는 것을 **반환(return)** 이라 하며, return을 만나는 순간 그 함수는 즉시 종료됩니다.
- 함수 안에서 또 다른 함수를 부르면 점프가 겹겹이 쌓입니다. 가장 나중에 호출된 함수가 가장 먼저 끝나는 구조로, **호출 스택(Call Stack)** 이 이를 관리합니다.

### 실기 문제 읽는 요령

실기 코드 문제는 "이 코드의 출력 결과를 쓰시오" 형태가 대부분입니다. 다음 순서로 접근하면 실수가 줄어듭니다.

- 먼저 **main(또는 시작점)을 찾는다**. 위에 함수가 많아도 시작점부터 읽습니다.
- 종이에 **변수 추적표**를 그린다. 변수 이름을 가로로 적고, 값이 바뀔 때마다 아래로 기록합니다.
- 반복문은 **매 회전마다** 변수 값을 표에 적습니다. 머릿속 암산은 반드시 틀립니다.
- 출력문을 만나면 그 시점의 변수 값을 **답안 칸에 즉시 옮겨 적습니다**.
- 출력 형식(줄바꿈 유무, 쉼표, 공백)까지 문제 코드와 똑같이 씁니다. 실기는 **부분 점수가 없는 단답형**이 많아 형식 실수가 곧 오답입니다.

### 변수 추적표 예시

int a = 1; int b = 2; a = a + b; b = a - b; 를 추적하면 아래와 같습니다.

| 실행한 줄 | a | b |
|---|---|---|
| a = 1, b = 2 | 1 | 2 |
| a = a + b | 3 | 2 |
| b = a - b | 3 | 1 |

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 시작점 찾기 | 함수 정의는 건너뛰고 main부터 읽는다 |
| return의 의미 | 만나는 즉시 함수 종료, 이후 줄은 실행 안 됨 |
| 변수 추적표 | 반복·호출이 섞이면 반드시 표로 기록 |
| 출력 형식 | 줄바꿈·공백·쉼표까지 코드와 동일하게 답안 작성 |
`,
    },
    {
      id: 'common-variable',
      name: '변수 선언과 초기화',
      topics: ['선언·초기화·할당의 구분', '쓰레기값', '언어별 차이'],
      content: `## 변수 선언과 초기화

**변수(Variable)** 는 값을 담아 두는 "이름표 붙은 상자"입니다. 상자를 만드는 일, 처음 값을 넣는 일, 값을 바꿔 넣는 일은 각각 다른 용어로 부르며, 실기 서술형에서 용어를 구분해 쓰는 것이 중요합니다.

### 세 가지 동작 구분

- **선언(Declaration)**: 상자를 만들고 이름표를 붙이는 것. 예를 들어 C에서 int a; 는 정수용 상자 a를 만든 것입니다.
- **초기화(Initialization)**: 상자를 만들면서 **처음으로** 값을 넣는 것. int a = 5; 는 선언과 초기화를 동시에 한 것입니다.
- **할당(Assignment, 대입)**: 이미 있는 상자의 값을 바꾸는 것. a = 10; 처럼 = 연산자로 값을 넣습니다.

여기서 = 는 수학의 "같다"가 아니라 **오른쪽 값을 왼쪽 상자에 넣어라**는 명령입니다. 그래서 a = a + 1; 처럼 수학적으로 이상해 보이는 식도 "a의 현재 값에 1을 더해 다시 a에 넣어라"로 읽으면 자연스럽습니다.

### 쓰레기값 — 초기화를 빼먹으면 생기는 일

- C에서 지역 변수를 선언만 하고 값을 넣지 않으면, 그 상자에는 이전에 메모리에 남아 있던 정체불명의 값이 들어 있습니다. 이를 **쓰레기값(Garbage Value)** 이라고 합니다.
- 이사 간 집의 냉장고를 열었더니 전 주인이 두고 간 음식이 있는 상황과 같습니다. 무엇이 들어 있을지 알 수 없습니다.
- Java는 초기화 없는 지역 변수를 **사용하려 하면 컴파일 오류**를 내고, 멤버 변수(필드)는 0·false·null 등 **기본값으로 자동 초기화**합니다.
- Python은 선언이라는 단계가 따로 없고, **값을 처음 할당하는 순간 변수가 생성**됩니다. 값 없이 이름만 쓰면 NameError가 납니다.

### 언어별 표기 비교

| 구분 | C | Java | Python |
|---|---|---|---|
| 선언+초기화 | int a = 5; | int a = 5; | a = 5 |
| 자료형 표기 | 필수 | 필수 | 없음(자동 판단) |
| 미초기화 지역변수 | 쓰레기값 | 컴파일 오류 | 변수 자체가 없음 |
| 문장 끝 기호 | 세미콜론 | 세미콜론 | 없음(줄바꿈) |

### 상수 — 바꿀 수 없는 상자

- 한 번 넣으면 바꿀 수 없는 값을 **상수(Constant)** 라고 합니다.
- C는 const int MAX = 100; Java는 final int MAX = 100; 으로 선언합니다.
- 상수 이름은 관례상 대문자로 씁니다. 실기 빈칸 문제에서 const, final 키워드가 자주 출제됩니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 용어 구분 | 선언·초기화·할당(대입)을 정확한 용어로 답한다 |
| = 의 의미 | 같다가 아니라 대입. a = a + 1 읽는 법 |
| 쓰레기값 | C 지역 변수는 초기화 전 값이 보장되지 않음 |
| 키워드 빈칸 | const(C), final(Java)은 상수 선언 키워드 |
`,
    },
    {
      id: 'common-datatype',
      name: '자료형',
      topics: ['정수·실수·문자·문자열·논리형', '언어별 표기 비교', '형 변환'],
      content: `## 자료형

**자료형(Data Type)** 은 상자에 "무엇을 담을 수 있는지" 정해 둔 규격입니다. 계란은 계란판에, 우유는 우유팩에 담듯이, 정수·실수·문자는 각각 알맞은 규격의 상자에 담습니다. 규격이 다르면 담기는 방식과 차지하는 크기가 달라집니다.

### 다섯 가지 기본 분류

- **정수형(Integer)**: 소수점 없는 수. 나이, 개수처럼 딱 떨어지는 값.
- **실수형(Floating Point)**: 소수점 있는 수. 키 172.5처럼 소수가 필요한 값. **부동소수점** 방식으로 저장되어 미세한 오차가 있을 수 있습니다.
- **문자형(Character)**: 글자 **한 개**. C·Java에서는 작은따옴표로 'A' 처럼 씁니다.
- **문자열(String)**: 글자 여러 개의 나열. 큰따옴표로 "Hello" 처럼 씁니다. C는 문자열 전용 자료형이 없어 **문자 배열(char 배열)** 로 다룹니다.
- **논리형(Boolean)**: 참(true)/거짓(false) 딱 두 가지. 전등 스위치의 ON/OFF와 같습니다.

### 언어별 표기 비교표

| 분류 | C | Java | Python |
|---|---|---|---|
| 정수 | int, long | int, long | int |
| 실수 | float, double | float, double | float |
| 문자 | char | char | 없음(길이 1 문자열) |
| 문자열 | char 배열 | String | str |
| 논리 | 0과 1로 표현 | boolean | bool(True/False) |

### 크기와 표현 범위 (실기 단골)

- C·Java의 **int는 4바이트(32비트)** 로, 대략 -21억 ~ +21억을 표현합니다.
- **char는 C에서 1바이트**, Java에서는 2바이트(유니코드)입니다.
- **float는 4바이트, double은 8바이트**이며 double이 더 정밀합니다.
- 표현 범위를 넘는 값을 넣으면 **오버플로(Overflow)** 가 발생해 엉뚱한 값이 됩니다. 주행거리계가 최대치를 넘으면 0으로 되돌아가는 것과 같은 원리입니다.

### 형 변환(Type Conversion)

- **암시적(자동) 형 변환**: 작은 상자 값을 큰 상자에 넣을 때 자동으로 일어납니다. int를 double에 대입하면 3이 3.0이 됩니다.
- **명시적(강제) 형 변환, 캐스팅(Casting)**: 프로그래머가 직접 지시합니다. C·Java에서 (int)3.7 은 소수점을 **버림**하여 3이 됩니다. 반올림이 아니라 **버림**인 점이 실기 함정입니다.
- Python은 int(3.7), float(3) 같은 함수 형태로 변환합니다. int(3.7)도 결과는 3입니다.

### 문자와 숫자의 관계

- 문자는 내부적으로 **아스키(ASCII) 코드** 숫자로 저장됩니다. 'A'는 65, 'a'는 97, '0'은 48입니다.
- 그래서 C·Java에서 'A' + 1 은 66이 되고, 이를 문자로 바꾸면 'B'가 됩니다. 문자 연산 문제의 핵심 원리입니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 크기 암기 | int 4바이트, char 1바이트(C), double 8바이트 |
| 캐스팅 결과 | (int)3.7 은 3 — 반올림 아닌 버림 |
| 아스키 코드 | 'A'=65, 'a'=97, '0'=48 |
| C의 문자열 | 전용 타입 없음, char 배열 + 널 문자로 표현 |
`,
    },
    {
      id: 'common-operator',
      name: '연산자',
      topics: ['산술·비교·논리·비트·삼항 연산자', '우선순위', '정수 나눗셈과 나머지'],
      content: `## 연산자

**연산자(Operator)** 는 값을 가공하는 도구입니다. 계산기 버튼처럼 종류별로 역할이 정해져 있고, 어떤 버튼을 먼저 누르는지(우선순위)에 따라 결과가 달라집니다.

### 산술 연산자 — 실기 최빈출

| 연산자 | 이름 | 예 | 결과 |
|---|---|---|---|
| + - * | 덧셈·뺄셈·곱셈 | 7 * 2 | 14 |
| / | 나눗셈 | 7 / 2 | 3 (정수끼리면 몫만) |
| % | 나머지(모듈로) | 7 % 2 | 1 |

- C·Java에서 **정수 / 정수 = 정수(몫)** 입니다. 7 / 2 는 3.5가 아니라 **3**. 소수점 이하는 버려집니다. 실기에서 가장 많이 틀리는 지점입니다.
- **% (나머지 연산)** 은 나누고 남은 것을 구합니다. 사탕 7개를 2명에게 나누면 1개가 남는 것과 같습니다. **짝수 판정(n % 2 == 0)**, 배수 판정, 순환 인덱스에 쓰입니다.
- Python의 / 는 항상 실수 결과(7 / 2 는 3.5)이고, 몫은 // 연산자(7 // 2 는 3)로 따로 구합니다.

### 비교 연산자

- 크기를 비교해 **참/거짓**을 돌려줍니다: 초과, 미만, 이상, 이하, 같음(==), 다름(!=).
- **= 는 대입, == 는 비교**입니다. if (a = 5) 처럼 잘못 쓰면 C에서는 대입이 일어나 항상 참이 되는 함정 문제가 출제됩니다.

### 논리 연산자

- **AND(&&)**: 둘 다 참일 때만 참. "숙제도 끝나고 방 청소도 끝나야 게임 가능."
- **OR(두 개의 세로 막대)**: 하나라도 참이면 참.
- **NOT(!)**: 참↔거짓 뒤집기.
- **단락 평가(Short-circuit)**: && 는 앞이 거짓이면 뒤를 **아예 계산하지 않고**, OR는 앞이 참이면 뒤를 계산하지 않습니다. 뒤쪽에 증감 연산이 있으면 실행 여부가 달라지는 함정으로 출제됩니다.

### 비트 연산자

정수를 2진수로 펼쳐 **자리(비트) 단위**로 계산합니다.

| 연산자 | 이름 | 예 (6=110, 3=011) | 결과 |
|---|---|---|---|
| & | 비트 AND | 6 & 3 | 2 (010) |
| ^ | 비트 XOR | 6 ^ 3 | 5 (101) |
| ~ | 비트 NOT | ~6 | -7 |
| 왼쪽 시프트 | a << n | 2 << 3 | 16 |
| 오른쪽 시프트 | a >> n | 16 >> 2 | 4 |

- 비트 OR(세로 막대 1개)는 6과 3에 적용하면 7(111)입니다.
- **시프트 공식**: 왼쪽 시프트 n칸 = 2의 n제곱 **곱하기**, 오른쪽 시프트 n칸 = 2의 n제곱으로 **나누기**. ~a 는 **-(a+1)** 로 계산하면 빠릅니다.

### 삼항 연산자

- 형태: 조건 ? 참일때값 : 거짓일때값
- 예: max = (a > b) ? a : b; 는 "a가 크면 a, 아니면 b를 max에 넣어라"는 한 줄 if문입니다.

### 우선순위 — 높은 것부터

| 순위 | 연산자 | 비고 |
|---|---|---|
| 1 | 괄호 ( ) | 무조건 최우선 |
| 2 | 단항: ! ~ ++ -- | 부호 포함 |
| 3 | * / % | 곱셈 계열 |
| 4 | + - | 덧셈 계열 |
| 5 | << >> | 시프트 |
| 6 | 비교 연산자 | == != 는 부등호보다 뒤 |
| 7 | 비트 & ^ 그리고 비트 OR | 이 순서대로 |
| 8 | && 그리고 논리 OR | 이 순서대로 |
| 9 | 삼항 ? : | |
| 10 | 대입 = 복합대입 | 가장 마지막 |

헷갈리면 **괄호가 최우선, 대입이 최후순위**만 확실히 기억하고 나머지는 산수 규칙(곱셈이 덧셈보다 먼저)을 따르면 대부분 맞습니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 정수 나눗셈 | C·Java의 7 / 2 는 3 — 몫만 남음 |
| 나머지 활용 | n % 2 == 0 은 짝수 판정 |
| 시프트 공식 | 왼쪽 n칸=곱하기 2ⁿ, 오른쪽 n칸=나누기 2ⁿ |
| 단락 평가 | && 앞이 거짓이면 뒤쪽 식은 실행 안 됨 |
| = vs == | 대입과 비교 혼동 함정 주의 |
`,
    },
    {
      id: 'common-incdec',
      name: '증감 연산자 전위·후위',
      topics: ['++a와 a++의 차이', '평가 순서', '식 안에서의 함정'],
      content: `## 증감 연산자 전위·후위

**증감 연산자(Increment/Decrement Operator)** 는 변수를 1씩 늘리거나(++) 줄이는(--) 연산자입니다. 단독으로 쓰면 아무 문제가 없지만, **다른 식 안에 섞여 있을 때** 전위/후위에 따라 결과가 달라져 실기 단골 함정이 됩니다.

### 전위와 후위의 정의

- **전위(Prefix) ++a**: **먼저 1 증가시키고**, 증가된 값을 식에 사용합니다. "선불" — 돈을 먼저 내고 물건을 받습니다.
- **후위(Postfix) a++**: **현재 값을 식에 먼저 사용하고**, 그 다음에 1 증가시킵니다. "후불" — 물건을 먼저 받고 나중에 계산합니다.
- 어느 쪽이든 **문장이 끝난 뒤 a의 값은 똑같이 1 증가**해 있습니다. 차이는 오직 "그 식 안에서 어떤 값이 쓰였는가"입니다.

### 기본 비교 예제

a가 5일 때를 가정합니다.

| 코드 | b에 들어가는 값 | 실행 후 a |
|---|---|---|
| b = ++a; | 6 | 6 |
| b = a++; | 5 | 6 |
| b = --a; | 4 | 4 |
| b = a--; | 5 | 4 |

### 출력문 안의 증감 — 최다 출제 유형

- printf("%d", a++); 는 **현재 값 5를 출력**한 뒤 a가 6이 됩니다.
- printf("%d", ++a); 는 a를 6으로 만든 뒤 **6을 출력**합니다.
- 연속 출력 문제: a=5 에서 printf("%d %d", a++, a++); 같은 코드는 언어·컴파일러마다 결과가 달라질 수 있어, 실기에서는 보통 한 식에 하나씩만 등장합니다. 하나씩 차분히 추적하면 됩니다.

### 복합식 추적 연습

x = 3 일 때 y = x++ + ++x; 를 추적해 봅니다.

- x++ 평가: 값 **3**을 사용하기로 하고, x는 4가 됩니다.
- ++x 평가: x를 5로 만들고 값 **5**를 사용합니다.
- y = 3 + 5 = **8**, 최종 x는 **5**입니다.

왼쪽에서 오른쪽으로, 만나는 순서대로 "사용값"과 "변수의 새 값"을 표로 나눠 적는 것이 정확합니다.

### 반복문 조건 속의 증감

- while (a++ < 3) 은 **비교를 먼저(현재 값으로)** 하고 나서 a를 증가시킵니다.
- while (++a < 3) 은 **증가부터** 시킨 값으로 비교합니다. 반복 횟수가 1회 달라지므로, 조건식 안 증감은 반드시 추적표로 확인합니다.

### 언어별 주의

- C·Java·JavaScript 모두 전위/후위를 지원합니다.
- **Python에는 ++ / -- 연산자가 없습니다.** a += 1 로 씁니다. Python 코드에 ++a 가 보이면 "양수 부호 두 번"으로 해석될 뿐 증가가 아닙니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 전위 ++a | 먼저 증가 후 사용 (선불) |
| 후위 a++ | 먼저 사용 후 증가 (후불) |
| 문장 종료 후 | 전위든 후위든 변수는 1 증가된 상태 |
| 조건식 속 증감 | 비교 시점의 값이 전위/후위에 따라 다름 — 반복 횟수 함정 |
| Python | 증감 연산자 없음, += 1 사용 |
`,
    },
    {
      id: 'common-control',
      name: '제어문 (조건 분기)',
      topics: ['if-else 사다리', 'switch-case와 break', '중첩 조건 읽기'],
      content: `## 제어문 (조건 분기)

**조건문(Conditional Statement)** 은 코드의 갈림길입니다. "비가 오면 우산을 챙기고, 아니면 그냥 나간다"처럼, 조건의 참/거짓에 따라 실행할 길을 고릅니다.

### if - else if - else 사다리

- **if**: 조건이 참이면 블록 실행.
- **else if**: 앞 조건이 거짓일 때 다음 조건 검사. 여러 개 이어 붙일 수 있습니다.
- **else**: 위의 모든 조건이 거짓일 때 실행되는 "나머지 전부" 칸.
- 핵심 규칙: 사다리에서는 **위에서부터 검사해 처음 참이 되는 블록 하나만 실행**되고, 나머지는 모두 건너뜁니다. 참인 조건이 여러 개여도 **가장 위의 것 하나만** 선택됩니다.

점수 등급 사다리 예시: if (score >= 90) 이 A, else if (score >= 80) 이 B라면, score가 95일 때 A만 출력됩니다. 95는 80 이상이기도 하지만 이미 위에서 걸렸기 때문입니다.

### 블록 중괄호 함정

- C·Java에서 if 뒤에 중괄호가 없으면 **바로 다음 한 문장만** if의 영향을 받습니다.
- if (a > 5) b = 1; c = 2; 에서 c = 2; 는 조건과 무관하게 **항상 실행**됩니다. 들여쓰기에 속지 말아야 합니다.
- Python은 반대로 **들여쓰기 자체가 블록**입니다. 들여쓰기가 같은 줄까지가 한 덩어리입니다.

### switch-case — 값으로 갈아타는 분기

switch는 변수 값에 따라 해당 **case 라벨로 점프**하는 구조입니다. 엘리베이터에서 층 버튼을 누르면 그 층으로 바로 가는 것과 같습니다.

- 각 case 끝의 **break** 는 switch를 빠져나가는 문입니다.
- **break가 없으면 다음 case로 계속 흘러내립니다.** 이를 **폴스루(Fall-through)** 라고 하며, 실기 최다 출제 함정입니다.
- **default** 는 일치하는 case가 없을 때 실행되는 칸(if의 else 역할)입니다.

| 코드 상황 (a=2) | break 있음 | break 없음 |
|---|---|---|
| case 1: 출력 1 | 건너뜀 | 건너뜀 |
| case 2: 출력 2 | 2 출력 후 종료 | 2 출력 후 아래로 |
| case 3: 출력 3 | 실행 안 됨 | 3도 출력 |
| default: 출력 9 | 실행 안 됨 | 9도 출력 |

break 없는 switch 문제는 **일치한 case부터 끝까지 전부 실행**한다고 기억하면 됩니다.

### 중첩 조건 읽는 법

- if 안에 또 if가 있으면, **바깥 조건이 참일 때만** 안쪽 조건을 검사합니다. 대문을 통과해야 방문을 열어볼 수 있는 이중 문입니다.
- **else는 가장 가까운 짝 없는 if와 결합**합니다(dangling else). 들여쓰기가 아니라 문법 규칙이 기준입니다.
- 복잡한 중첩은 조건마다 참/거짓을 메모하며 **한 갈래만 따라가면** 됩니다. 선택되지 않은 갈래는 읽을 필요가 없습니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 사다리 규칙 | 처음 참이 된 블록 하나만 실행 |
| 중괄호 없는 if | 바로 다음 한 문장만 조건의 영향권 |
| 폴스루 | break 없는 case는 아래로 계속 실행 |
| default | 일치 case 없을 때 실행되는 기본 칸 |
| else 결합 | 가장 가까운 if와 짝 — 들여쓰기에 속지 않기 |
`,
    },
    {
      id: 'common-loop',
      name: '반복문',
      topics: ['for·while 실행 횟수', 'break·continue', '중첩 루프 총 횟수 계산'],
      content: `## 반복문

**반복문(Loop)** 은 같은 일을 여러 번 시키는 구조입니다. "운동장 10바퀴 돌기"처럼 횟수가 정해진 반복은 **for**, "지칠 때까지 돌기"처럼 조건이 유지되는 동안의 반복은 **while**이 어울립니다.

### for문의 실행 순서

for (초기식; 조건식; 증감식) { 본문 } 의 실제 순서는 다음과 같습니다.

- **초기식** — 처음 딱 한 번 실행.
- **조건식** 검사 — 참이면 본문 실행, 거짓이면 반복 종료.
- 본문 실행 후 **증감식** 실행, 다시 조건식으로.
- 즉 순서는 "초기 → (조건 → 본문 → 증감) 반복"이며, **조건 검사가 본문보다 항상 먼저**입니다.

### 실행 횟수 세는 공식

- for (i = 0; i < n; i++) 는 **n회** 실행됩니다 (0부터 n-1까지).
- for (i = 1; i <= n; i++) 도 **n회** 입니다 (1부터 n까지).
- 일반화하면 시작값부터 끝값까지 **(끝값 - 시작값) / 증가폭 + 1** 회입니다. i = 2; i <= 10; i += 2 는 (10-2)/2+1 = **5회**.
- 반복 종료 후 i 값도 자주 물어봅니다. i < n 조건이면 종료 시점의 i는 **n**(조건을 처음 깬 값)입니다.

### while과 do-while의 차이

- **while**: 조건을 **먼저** 검사. 조건이 처음부터 거짓이면 **0회 실행**.
- **do-while**: 본문을 **먼저 한 번 실행하고** 조건을 검사. 조건이 거짓이어도 **최소 1회는 실행**됩니다. 놀이기구를 일단 한 번 태워 주고 나서 더 탈지 묻는 방식입니다.
- do-while 문제에서 "조건이 거짓인데 왜 한 번 실행되지?"가 바로 출제 의도입니다.

### break와 continue

- **break**: 반복문을 **즉시 탈출**합니다. 경기 중 부상으로 퇴장하는 것.
- **continue**: 이번 회차의 남은 본문을 건너뛰고 **다음 회차로** 넘어갑니다. 이번 바퀴만 걷고 다음 바퀴는 다시 뛰는 것.
- 중첩 루프에서 break는 **자신을 감싼 가장 안쪽 반복문 하나만** 탈출합니다. 바깥 루프는 계속 돕니다.

### 중첩 루프 총 횟수 — 실기 단골

- 바깥 m회 × 안쪽 n회 = 본문 **m × n회** 실행. 바깥 3회, 안쪽 4회면 총 12회입니다.
- 안쪽 루프가 바깥 변수에 의존하는 삼각형 패턴은 등차수열 합으로 셉니다.

| 패턴 (i는 바깥, j는 안쪽) | 안쪽 실행 횟수 | 총합 (n=4일 때) |
|---|---|---|
| j = 0; j < n | 매번 n회 | 4+4+4+4 = 16 |
| j = 0; j < i (i는 0부터 n-1) | 0,1,2,3회 | 0+1+2+3 = 6 |
| j = i; j < n (i는 0부터 n-1) | 4,3,2,1회 | 4+3+2+1 = 10 |

- 1부터 n까지의 합 공식 **n(n+1)/2** 를 외워 두면 삼각형 패턴을 즉시 계산할 수 있습니다.
- 별 찍기, 구구단, 2차원 배열 순회가 모두 이 구조입니다. **바깥 한 바퀴마다 안쪽이 처음부터 끝까지 다시 돈다**는 감각이 핵심입니다.

### 무한 루프

- while (1), for (;;) 는 조건이 항상 참인 **무한 루프(Infinite Loop)** 입니다. 내부의 break가 유일한 출구이므로, break 조건을 찾는 것이 문제 풀이의 열쇠입니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 횟수 공식 | (끝-시작)/증가폭 + 1 |
| 종료 후 변수값 | 조건을 처음 깨뜨린 값 (i < n 이면 n) |
| do-while | 최소 1회 실행 보장 |
| break 범위 | 가장 안쪽 반복문 하나만 탈출 |
| 중첩 총 횟수 | 독립이면 m×n, 삼각형이면 n(n+1)/2 |
`,
    },
    {
      id: 'common-scope-memory',
      name: '변수 스코프와 메모리',
      topics: ['전역·지역 변수', '스택과 힙', '값 전달 vs 참조 전달'],
      content: `## 변수 스코프와 메모리

**스코프(Scope, 유효 범위)** 는 "이 변수 이름이 통하는 구역"입니다. 학교 방송은 학교 안에서만 들리고, 교실 스피커는 그 교실에서만 들리는 것과 같습니다.

### 전역 변수와 지역 변수

- **전역 변수(Global Variable)**: 함수 바깥에 선언. 프로그램 전체에서 접근 가능하고, 프로그램이 끝날 때까지 살아 있습니다. C·Java에서는 **0으로 자동 초기화**됩니다.
- **지역 변수(Local Variable)**: 함수(또는 블록) 안에 선언. 그 블록 안에서만 보이고, 블록이 끝나면 **소멸**합니다. C에서는 자동 초기화되지 않아 쓰레기값을 가질 수 있습니다.
- **이름이 겹치면 지역이 이깁니다(섀도잉, Shadowing).** 전역 a와 지역 a가 있으면 함수 안에서는 지역 a가 우선입니다. 실기에서 "함수 안 출력과 밖 출력이 왜 다른가"의 정답이 이것입니다.
- 함수가 끝나면 지역 변수의 변경은 사라지므로, 함수 밖 전역 값은 (전역을 직접 고치지 않는 한) 그대로입니다.

### 메모리 구역 — 스택과 힙

프로그램의 메모리는 용도별 구역으로 나뉩니다.

| 구역 | 담는 것 | 특징 |
|---|---|---|
| 코드(Code) 영역 | 실행할 명령어 | 읽기 전용 |
| 데이터(Data) 영역 | 전역·정적(static) 변수 | 프로그램 종료까지 유지 |
| 스택(Stack) | 지역 변수, 매개변수, 복귀 주소 | 함수 호출 시 쌓이고 종료 시 자동 제거 |
| 힙(Heap) | 동적 할당 메모리 | 프로그래머(또는 GC)가 관리 |

- **스택**은 식판 더미입니다. 함수를 호출하면 그 함수의 작업 공간(스택 프레임)이 위에 쌓이고, 함수가 끝나면 맨 위 것부터 치워집니다. **후입선출(LIFO)** 구조입니다.
- **힙**은 창고입니다. 필요할 때 빌리고(malloc, new) 다 쓰면 반납합니다. C는 free로 직접 반납해야 하며, 반납을 잊으면 **메모리 누수(Memory Leak)** 가 생깁니다. Java·Python은 **가비지 컬렉터(Garbage Collector)** 가 안 쓰는 메모리를 자동 회수합니다.
- 재귀 호출이 너무 깊어 스택이 넘치는 것이 **스택 오버플로(Stack Overflow)** 입니다.

### 값 전달 vs 참조 전달 — 실기 최다 출제

함수에 인자를 넘기는 방식에 따라 원본이 바뀌는지가 결정됩니다.

- **값 전달(Call by Value)**: 값의 **복사본**을 넘깁니다. 문서를 복사해서 준 것이라, 받은 쪽이 낙서해도 **원본은 무사**합니다. C의 기본 방식이며, swap(a, b) 함수가 원본을 못 바꾸는 이유입니다.
- **참조 전달(Call by Reference)**: 원본의 **주소**를 넘깁니다. 문서가 있는 사물함 열쇠를 준 것이라, 받은 쪽이 고치면 **원본이 바뀝니다**. C는 포인터(&a를 넘기고 *p로 접근)로 이를 구현합니다.
- Java는 항상 값 전달이지만, 객체를 넘길 때는 **참조값(주소)이 복사**되므로 배열·객체의 내용 변경은 호출자에게도 보입니다. 배열을 받아 원소를 바꾸는 문제가 이 유형입니다.
- Python도 같은 원리입니다. 리스트처럼 **변경 가능한(mutable)** 객체는 함수 안 수정이 밖에 반영되고, 숫자·문자열처럼 **변경 불가능한(immutable)** 값은 반영되지 않습니다.

### 판단 요령

- 넘긴 것이 **낱값(int, double 등)** 이면: 함수 안 변경은 밖에 영향 없음.
- 넘긴 것이 **배열·객체·포인터**면: 내용 변경이 밖에도 반영됨.
- 출력 문제에서 함수 호출 전후의 변수 값을 각각 물으면, 이 기준 하나로 대부분 판정됩니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 섀도잉 | 이름이 겹치면 지역 변수가 전역을 가림 |
| 자동 초기화 | 전역·static은 0, C 지역 변수는 쓰레기값 |
| 스택/힙 | 스택=지역변수·LIFO, 힙=동적할당·GC 또는 free |
| 값 전달 | 복사본 전달 — swap이 원본을 못 바꾸는 이유 |
| 배열 전달 | 주소가 전달되어 함수 안 수정이 원본에 반영 |
`,
    },
  ],
}

export const sqlCodingArea: TheoryArea = {
  id: 'sql-theory',
  name: 'SQL',
  icon: 'fa-solid fa-table',
  description: '실기 SQL 작성·빈칸·결과 예측 문제 대비. SELECT 실행 순서부터 DDL·DCL까지 답안에 그대로 쓰는 문법을 정리한다.',
  chapters: [
    {
      id: 'sql-select-basic',
      name: 'SELECT 기본 구조',
      topics: ['절의 작성 순서', '논리적 실행 순서', 'DISTINCT와 별칭'],
      content: `## SELECT 기본 구조

**SELECT문**은 표(테이블)에서 원하는 데이터를 꺼내 오는 명령입니다. 도서관에서 "소설 코너에서, 2020년 이후 출간된 책만, 제목순으로 정리해 주세요"라고 요청하는 것과 같습니다.

### 여섯 개의 절 — 작성 순서

- **SELECT** 컬럼목록 — 무엇을 볼지
- **FROM** 테이블 — 어디서 가져올지
- **WHERE** 조건 — 어떤 행만 남길지
- **GROUP BY** 컬럼 — 무엇을 기준으로 묶을지
- **HAVING** 조건 — 묶은 그룹 중 어떤 그룹만 남길지
- **ORDER BY** 컬럼 — 결과를 어떤 순서로 보여줄지

작성(문법) 순서는 위 그대로이며, 순서를 바꿔 쓰면 오류입니다. 빈칸 문제에서 절의 위치를 묻는 경우가 많습니다.

### 논리적 실행 순서 — 실기 최빈출

데이터베이스가 실제로 처리하는 순서는 작성 순서와 다릅니다.

| 실행 순서 | 절 | 하는 일 |
|---|---|---|
| 1 | FROM | 대상 테이블을 가져온다 |
| 2 | WHERE | 조건에 맞는 행만 남긴다 |
| 3 | GROUP BY | 남은 행을 그룹으로 묶는다 |
| 4 | HAVING | 그룹에 조건을 걸러낸다 |
| 5 | SELECT | 보여줄 컬럼을 고른다 |
| 6 | ORDER BY | 결과를 정렬한다 |

요리에 비유하면 재료 창고에서 꺼내고(FROM), 상한 것을 골라내고(WHERE), 종류별로 나누고(GROUP BY), 나눈 묶음 중 쓸 것만 남기고(HAVING), 접시에 담고(SELECT), 보기 좋게 배열(ORDER BY)하는 순서입니다.

- 이 순서 때문에 **WHERE에서는 SELECT의 별칭(Alias)을 쓸 수 없습니다.** WHERE가 SELECT보다 먼저 실행되기 때문입니다.
- 반대로 **ORDER BY는 별칭을 쓸 수 있습니다.** SELECT 다음에 실행되기 때문입니다.
- "실행 순서를 기호로 나열하시오"가 그대로 출제됩니다. **FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY** 를 통째로 암기해야 합니다.

### 자주 쓰는 기본 문법

- 모든 컬럼 조회: SELECT * FROM 사원;
- 특정 컬럼 조회: SELECT 이름, 급여 FROM 사원;
- **DISTINCT**: 중복 행 제거. SELECT DISTINCT 부서 FROM 사원; 은 같은 부서명을 한 번씩만 보여줍니다.
- **별칭(AS)**: SELECT 급여 * 12 AS 연봉 FROM 사원; 처럼 결과 컬럼에 새 이름을 붙입니다. AS는 생략 가능합니다.
- **ORDER BY 정렬 방향**: **ASC(오름차순, 기본값)**, **DESC(내림차순)**. SELECT 이름 FROM 사원 ORDER BY 급여 DESC; 는 급여가 높은 순입니다. 정렬 방향 빈칸으로 DESC가 자주 출제됩니다.
- 여러 기준 정렬: ORDER BY 부서 ASC, 급여 DESC 는 부서로 먼저 정렬하고, 같은 부서 안에서 급여 내림차순입니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 실행 순서 암기 | FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY |
| 별칭 사용 가능 위치 | WHERE 불가, ORDER BY 가능 |
| DISTINCT | 중복 제거 키워드 — 빈칸 단골 |
| 기본 정렬 | ORDER BY의 기본은 ASC, 내림차순은 DESC 명시 |
`,
    },
    {
      id: 'sql-where',
      name: 'WHERE 조건',
      topics: ['비교·논리 조건', 'LIKE 와일드카드', 'BETWEEN·IN·IS NULL'],
      content: `## WHERE 조건

**WHERE절**은 행을 걸러내는 체입니다. 조건에 맞는 행만 통과시키고 나머지는 버립니다. 조건을 정확히 쓰는 것이 실기 SQL 작성 문제의 절반입니다.

### 비교 연산자

- 같음은 **=** 하나만 씁니다 (프로그래밍의 == 와 다름).
- 다름은 **<> 또는 !=** 로 씁니다. 표준은 <> 입니다.
- 크기 비교는 부등호 그대로: 급여 >= 3000 등.
- 문자열 비교는 작은따옴표로 감쌉니다: WHERE 부서 = '영업부'

### 논리 연산자로 조건 결합

- **AND**: 두 조건 모두 만족. WHERE 부서 = '영업부' AND 급여 >= 3000
- **OR**: 하나만 만족해도 통과.
- **NOT**: 조건 뒤집기.
- 우선순위는 **NOT → AND → OR** 순입니다. AND가 OR보다 먼저 묶이므로, 의도가 다르면 괄호로 감싸야 합니다. 괄호 유무에 따라 결과 건수가 달라지는 문제가 출제됩니다.

### LIKE — 패턴 검색 (와일드카드)

**LIKE**는 문자열이 특정 패턴과 닮았는지 검사합니다. 와일드카드 두 가지가 핵심입니다.

| 와일드카드 | 의미 | 예 | 매칭되는 값 |
|---|---|---|---|
| % | 0글자 이상 아무거나 | '김%' | 김, 김철수, 김미 |
| _ | 정확히 1글자 | '김_' | 김수, 김호 (김철수는 안 됨) |

- '%수' 는 수로 **끝나는** 값, '%수%' 는 수가 **포함된** 값입니다.
- '_'는 언더스코어(밑줄)이며 글자 수를 정확히 지정할 때 씁니다. '__수' 는 세 글자이면서 수로 끝나는 값입니다.
- "성이 김씨인 사원"은 WHERE 이름 LIKE '김%' — 실기 작성 단골입니다.
- 패턴과 다르게 부정하려면 **NOT LIKE** 를 씁니다.

### BETWEEN — 범위 조건

- WHERE 급여 BETWEEN 2000 AND 4000 은 **2000 이상 4000 이하**입니다.
- **양 끝값을 포함**한다는 점이 핵심입니다. 급여 >= 2000 AND 급여 <= 4000 과 완전히 같습니다.
- 날짜에도 씁니다: WHERE 입사일 BETWEEN '2024-01-01' AND '2024-12-31'

### IN — 목록 중 하나

- WHERE 부서 IN ('영업부', '기획부') 는 목록에 있는 값 중 **하나라도 일치**하면 통과입니다.
- 부서 = '영업부' OR 부서 = '기획부' 를 짧게 쓴 것입니다.
- 부정은 **NOT IN** 입니다.

### NULL 판정 — IS NULL (실기 함정)

- **NULL은 "값이 없음"** 을 뜻하며 0도, 빈 문자열도 아닙니다. 아직 아무것도 적지 않은 빈칸입니다.
- NULL은 = 로 비교할 수 없습니다. **컬럼 = NULL 은 항상 거짓(불명)** 이 되어 아무 행도 나오지 않습니다.
- 반드시 **IS NULL / IS NOT NULL** 로 판정합니다: WHERE 전화번호 IS NULL
- "= NULL 이라고 쓴 오류를 찾아 고치시오" 유형과 IS NULL 빈칸 유형이 반복 출제됩니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| % 와 _ | %는 0글자 이상, _는 정확히 1글자 |
| BETWEEN | 양 끝값 포함 (이상~이하) |
| IN | 여러 OR 조건의 축약형 |
| NULL 판정 | = NULL 불가, IS NULL / IS NOT NULL 사용 |
| AND/OR 우선순위 | AND가 먼저 — 필요하면 괄호 |
`,
    },
    {
      id: 'sql-groupby',
      name: '집계 함수와 GROUP BY',
      topics: ['5대 집계 함수', 'COUNT(*)와 COUNT(컬럼)', 'HAVING vs WHERE'],
      content: `## 집계 함수와 GROUP BY

**집계 함수(Aggregate Function)** 는 여러 행을 하나의 요약값으로 만드는 함수입니다. 반 전체 시험지를 걷어 평균 하나를 내는 것과 같습니다. **GROUP BY**는 그 전에 "반별로 나눠서" 집계하게 하는 장치입니다.

### 5대 집계 함수

| 함수 | 의미 | 예 |
|---|---|---|
| COUNT | 행의 개수 | COUNT(*) |
| SUM | 합계 | SUM(급여) |
| AVG | 평균 | AVG(급여) |
| MAX | 최댓값 | MAX(급여) |
| MIN | 최솟값 | MIN(급여) |

### COUNT(*)와 COUNT(컬럼)의 차이 — 최다 출제

- **COUNT(*)**: 조건을 통과한 **모든 행**을 셉니다. NULL이 있어도 행이면 셉니다.
- **COUNT(컬럼)**: 그 컬럼이 **NULL이 아닌 행만** 셉니다.
- **COUNT(DISTINCT 컬럼)**: NULL을 제외하고 **중복까지 제거한** 개수입니다.

전화번호 컬럼에 5행 중 2행이 NULL이라면: COUNT(*) = 5, COUNT(전화번호) = 3 입니다. "결과 값을 쓰시오" 유형에서 NULL 행 수를 세게 하는 것이 출제 의도입니다. **SUM·AVG도 NULL을 계산에서 제외**하므로, AVG(컬럼)는 "NULL 아닌 값들의 합 ÷ NULL 아닌 값의 개수"입니다.

### GROUP BY — 그룹별로 묶기

- SELECT 부서, AVG(급여) FROM 사원 GROUP BY 부서; 는 부서별 평균 급여를 냅니다.
- 결과 행 수 = **그룹(서로 다른 부서 값)의 수**입니다.
- 중요한 문법 규칙: **SELECT에는 GROUP BY에 쓴 컬럼과 집계 함수만 올 수 있습니다.** 부서로 묶었는데 개인의 이름을 SELECT에 쓰면 오류입니다. 반별 평균표에 특정 학생 이름 칸이 있을 수 없는 것과 같습니다.

### HAVING vs WHERE — 거르는 시점이 다르다

- **WHERE**: 그룹으로 묶기 **전**, 개별 **행**을 거릅니다. 집계 함수를 쓸 수 없습니다.
- **HAVING**: 묶은 **후**, **그룹**을 거릅니다. 집계 함수 조건은 반드시 HAVING에 씁니다.
- WHERE AVG(급여) >= 3000 은 오류이고, HAVING AVG(급여) >= 3000 이 맞습니다. "틀린 절을 고치시오"와 HAVING 빈칸이 모두 단골입니다.

### 종합 예제

부서별 인원이 3명 이상인 부서의 부서명과 평균 급여를 급여 높은 순으로 조회:

SELECT 부서, AVG(급여) FROM 사원 GROUP BY 부서 HAVING COUNT(*) >= 3 ORDER BY AVG(급여) DESC;

- 행 필터(WHERE)가 필요하면 GROUP BY **앞**에, 그룹 필터(HAVING)는 GROUP BY **뒤**에 씁니다.
- 실행 순서(FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY)를 떠올리면 어느 절에 어떤 조건을 쓸지 자동으로 정해집니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| COUNT(*) vs COUNT(컬럼) | 컬럼 지정 시 NULL 행 제외 |
| 집계와 NULL | SUM·AVG도 NULL 제외하고 계산 |
| WHERE vs HAVING | 행 필터 vs 그룹 필터, 집계 조건은 HAVING |
| SELECT 제약 | GROUP BY 컬럼과 집계 함수만 SELECT 가능 |
| 결과 행 수 | 그룹의 개수 = 서로 다른 그룹 값의 수 |
`,
    },
    {
      id: 'sql-join',
      name: 'JOIN',
      topics: ['INNER·LEFT·RIGHT·FULL', '결과 건수 세는 법', 'ON과 WHERE'],
      content: `## JOIN

**조인(JOIN)** 은 두 테이블을 공통 컬럼으로 이어 붙여 하나의 표처럼 조회하는 기능입니다. 학생 명단과 동아리 가입 명단을 "학번"으로 맞대어 보는 것과 같습니다. 어느 쪽의 짝 없는 행까지 보여줄지에 따라 네 종류로 나뉩니다.

### 네 가지 JOIN

- **INNER JOIN(내부 조인)**: **양쪽 모두에 짝이 있는 행만** 결과에 나옵니다. 미팅에서 짝이 맺어진 사람만 입장.
- **LEFT OUTER JOIN**: 왼쪽 테이블의 행은 **짝이 없어도 전부** 나오고, 짝 없는 자리는 **NULL**로 채워집니다.
- **RIGHT OUTER JOIN**: 오른쪽 테이블 기준으로 같은 원리입니다.
- **FULL OUTER JOIN**: 양쪽 모두 짝 없는 행까지 전부 나옵니다.

기본 문법: SELECT * FROM A INNER JOIN B ON A.id = B.id; 이며, 연결 조건은 **ON** 뒤에 씁니다. OUTER는 생략 가능해 LEFT JOIN이라고만 써도 됩니다.

### 예시 데이터로 결과 건수 세기 — 실기 단골

학생 테이블(왼쪽)과 동아리 테이블(오른쪽)이 학번으로 연결된다고 합시다.

| 학생 테이블 학번 | 동아리 테이블 학번 |
|---|---|
| 100 | 100 |
| 200 | 200 |
| 300 | 400 |

- 짝이 맞는 학번: 100, 200 (2건)
- 왼쪽에만 있는 학번: 300 / 오른쪽에만 있는 학번: 400

| JOIN 종류 | 결과 건수 | 구성 |
|---|---|---|
| INNER | 2 | 짝 있는 100, 200 |
| LEFT | 3 | 100, 200 + 짝 없는 300(오른쪽 값 NULL) |
| RIGHT | 3 | 100, 200 + 짝 없는 400(왼쪽 값 NULL) |
| FULL | 4 | 100, 200, 300, 400 |

- 세는 공식: **INNER = 매칭 건수**, **LEFT = 매칭 + 왼쪽 홀로 행**, **RIGHT = 매칭 + 오른쪽 홀로 행**, **FULL = 매칭 + 양쪽 홀로 행 전부**.
- 한 학번이 상대 테이블의 **여러 행과 매칭되면 매칭되는 수만큼 행이 늘어납니다.** 왼쪽 1행이 오른쪽 3행과 짝이면 결과는 3행입니다. 건수 문제의 고급 함정입니다.

### 그 밖의 조인 용어 (용어 문제 대비)

- **CROSS JOIN(교차 조인)**: 조건 없이 **모든 조합**을 만듭니다. 결과 건수 = 왼쪽 행 수 × 오른쪽 행 수. **카티션 곱(Cartesian Product)** 이라고도 하며, 조인 조건을 빠뜨렸을 때 나타나는 현상으로도 출제됩니다.
- **셀프 조인(Self Join)**: 같은 테이블을 별칭 두 개로 자기 자신과 조인합니다. 사원 테이블에서 사원과 그 상사를 함께 보는 예가 대표적입니다.
- **등가 조인(Equi Join)**: = 로 연결하는 조인. **자연 조인(NATURAL JOIN)** 은 같은 이름의 컬럼으로 자동 연결하며 중복 컬럼이 한 번만 나옵니다.
- 구식 표기: FROM A, B WHERE A.id = B.id 처럼 WHERE에 조인 조건을 쓰는 형태도 INNER JOIN과 같은 결과입니다. 실기 지문에 혼용되어 나옵니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| INNER | 양쪽 매칭 행만 — 짝 없는 행 탈락 |
| OUTER의 NULL | 기준 테이블의 짝 없는 행은 상대 컬럼이 NULL |
| 건수 공식 | FULL = 매칭 + 왼쪽 홀로 + 오른쪽 홀로 |
| 다중 매칭 | 1:N 매칭이면 N행으로 늘어남 |
| 카티션 곱 | 조건 없는 조인, 건수 = m × n |
`,
    },
    {
      id: 'sql-subquery',
      name: '서브쿼리',
      topics: ['단일행·다중행 서브쿼리', 'IN·EXISTS', 'ANY·ALL'],
      content: `## 서브쿼리

**서브쿼리(Subquery, 하위 질의)** 는 SQL문 안에 괄호로 들어 있는 또 하나의 SELECT문입니다. "우리 반 평균보다 키 큰 사람?"이라는 질문에 답하려면 먼저 평균(안쪽 질문)을 구해야 하듯, **안쪽 쿼리가 먼저 실행되고 그 결과를 바깥 쿼리가 사용**합니다.

### 기본 형태

평균 급여보다 급여가 높은 사원 조회:

SELECT 이름 FROM 사원 WHERE 급여 > (SELECT AVG(급여) FROM 사원);

- 괄호 안이 먼저 실행되어 하나의 값(예: 3200)이 되고, 바깥 쿼리는 WHERE 급여 > 3200 처럼 동작합니다.
- 서브쿼리는 WHERE 외에 FROM(인라인 뷰), SELECT(스칼라 서브쿼리), HAVING에도 올 수 있습니다.

### 단일행 vs 다중행 — 연산자 선택 기준

- **단일행 서브쿼리**: 결과가 **1행 1값**. =, >, < 같은 일반 비교 연산자를 씁니다. 집계 함수 결과(AVG, MAX 등)가 대표적입니다.
- **다중행 서브쿼리**: 결과가 **여러 행**. 일반 비교 연산자를 쓰면 오류가 나며, 반드시 **IN, EXISTS, ANY, ALL** 같은 다중행 연산자를 써야 합니다.
- "오류의 원인을 쓰시오" 유형: 다중행 결과에 = 를 쓴 것이 정답인 경우가 많습니다.

### IN — 목록에 포함되는가

영업부가 있는 지역의 모든 사원 조회처럼, 서브쿼리가 여러 값을 돌려줄 때 씁니다.

SELECT 이름 FROM 사원 WHERE 부서번호 IN (SELECT 부서번호 FROM 부서 WHERE 지역 = '서울');

- 서브쿼리 결과 목록 중 **하나라도 일치하면** 통과. 부정은 **NOT IN**.
- NOT IN의 서브쿼리 결과에 NULL이 섞이면 아무 행도 안 나오는 것이 고급 함정입니다.

### EXISTS — 존재하는가

- **EXISTS**는 서브쿼리 결과가 **한 행이라도 존재하면 참**입니다. 값 비교가 아니라 **존재 여부**만 봅니다.
- 보통 바깥 행과 연결된 **상관 서브쿼리(Correlated Subquery)** 형태로 씁니다. 상관 서브쿼리는 바깥 쿼리의 행 하나마다 안쪽 쿼리가 반복 실행되는 구조입니다.
- 주문한 적 있는 고객: SELECT 이름 FROM 고객 c WHERE EXISTS (SELECT * FROM 주문 o WHERE o.고객id = c.고객id);
- 부정은 **NOT EXISTS** 이며 "한 번도 ~하지 않은" 조회에 씁니다.

### ANY와 ALL — 여럿과의 크기 비교

| 조건 | 의미 | 결과적으로 같은 것 |
|---|---|---|
| 급여 > ANY (목록) | 목록 중 **하나보다만 크면** 참 | 급여 > 목록의 최솟값 |
| 급여 > ALL (목록) | 목록 **전부보다 커야** 참 | 급여 > 목록의 최댓값 |
| 급여 < ANY (목록) | 하나보다만 작으면 참 | 급여 < 목록의 최댓값 |
| 급여 < ALL (목록) | 전부보다 작아야 참 | 급여 < 목록의 최솟값 |

- 서브쿼리 결과가 (2000, 3000, 4000)일 때, > ANY 는 2000 초과면 참, > ALL 은 4000 초과여야 참입니다.
- **ANY는 최소 하나만 이기면 되고, ALL은 전원을 이겨야 한다**로 기억합니다. SOME은 ANY와 같은 뜻입니다.
- "다음 두 쿼리의 결과 건수를 비교하시오" 유형으로 ANY/ALL이 출제됩니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 실행 순서 | 안쪽(서브쿼리) 먼저, 결과를 바깥이 사용 |
| 연산자 선택 | 단일행=비교연산자, 다중행=IN·EXISTS·ANY·ALL |
| EXISTS | 값이 아닌 존재 여부 판정, 상관 서브쿼리와 함께 |
| ANY vs ALL | 하나만 만족 vs 전부 만족 (최솟값/최댓값으로 환산) |
`,
    },
    {
      id: 'sql-dml',
      name: 'DML (INSERT·UPDATE·DELETE)',
      topics: ['세 명령의 문법', 'WHERE 누락의 위험', '실행 후 상태 추적'],
      content: `## DML (INSERT·UPDATE·DELETE)

**DML(Data Manipulation Language, 데이터 조작어)** 은 표 안의 **데이터(행)** 를 넣고, 고치고, 지우는 명령입니다. 표 자체(틀)를 바꾸는 DDL과 구분됩니다. SELECT까지 포함해 DML로 분류하는 것이 시험 기준입니다.

### INSERT — 행 추가

- 기본형: INSERT INTO 사원 (사번, 이름, 급여) VALUES (101, '김철수', 3000);
- 컬럼 목록을 **생략**하면 테이블의 **모든 컬럼 순서대로** 값을 넣어야 합니다: INSERT INTO 사원 VALUES (101, '김철수', 3000);
- 값을 지정하지 않은 컬럼에는 **NULL 또는 DEFAULT 값**이 들어갑니다.
- 문법 키워드 **INTO** 와 **VALUES** 가 빈칸으로 자주 출제됩니다.
- SELECT 결과를 통째로 넣을 수도 있습니다: INSERT INTO 백업사원 SELECT * FROM 사원 WHERE 부서 = '영업부';

### UPDATE — 행 수정

- 기본형: UPDATE 사원 SET 급여 = 4000 WHERE 사번 = 101;
- **SET** 뒤에 바꿀 내용, **WHERE** 뒤에 대상 조건입니다. SET이 빈칸 단골입니다.
- 여러 컬럼 동시 수정은 쉼표로: SET 급여 = 4000, 부서 = '기획부'
- 현재 값 기반 수정: UPDATE 사원 SET 급여 = 급여 * 1.1 WHERE 부서 = '영업부'; (영업부 10% 인상)
- **WHERE를 빼면 모든 행이 수정됩니다.** 전교생 성적을 한 명 고치려다 전부 고쳐 버리는 사고입니다. "이 문장의 문제점을 쓰시오" 유형의 정답이 됩니다.

### DELETE — 행 삭제

- 기본형: DELETE FROM 사원 WHERE 사번 = 101;
- **FROM** 키워드가 빈칸으로 출제됩니다.
- **WHERE를 빼면 모든 행이 삭제**되지만, **테이블 구조(틀)는 남습니다.** 공책의 내용만 지우고 공책은 남기는 것입니다.
- 구조까지 없애는 것은 DDL의 **DROP**, 구조는 남기고 전체 행을 빠르게 비우는 것은 **TRUNCATE** 입니다. DELETE(행 단위, 롤백 가능) vs TRUNCATE(전체, 롤백 불가) vs DROP(테이블 자체 제거) 3종 비교가 단골 출제입니다.

### 실행 후 상태 추적 문제 풀이법

실기에서는 "다음 SQL들을 순서대로 실행한 후 테이블 상태(또는 SELECT 결과)를 쓰시오"가 나옵니다.

- 초기 테이블을 종이에 그대로 옮겨 그립니다.
- 문장을 **한 개 실행할 때마다** 표를 갱신합니다: INSERT는 행 추가, UPDATE는 해당 칸 수정, DELETE는 행에 취소선.
- WHERE 조건에 맞는 행이 **몇 개인지 먼저 세고** 그 행들만 건드립니다. 조건에 맞는 행이 없으면 **아무 일도 일어나지 않습니다**(오류 아님).
- 마지막 SELECT의 조건·정렬까지 반영해 최종 답을 적습니다.

### 트랜잭션 제어와의 연결 (TCL)

- DML의 변경은 **COMMIT** 으로 확정하고, **ROLLBACK** 으로 되돌립니다. **SAVEPOINT** 는 부분 되돌리기 지점입니다.
- ROLLBACK 후 상태를 묻는 문제: 마지막 COMMIT 이후의 DML만 취소된다는 점이 핵심입니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| 키워드 빈칸 | INSERT INTO ~ VALUES, UPDATE ~ SET, DELETE FROM |
| WHERE 누락 | UPDATE·DELETE 전체 행 반영 — 위험성 서술 문제 |
| DELETE vs TRUNCATE vs DROP | 행 삭제·구조 유지 / 전체 비움 / 구조까지 제거 |
| 상태 추적 | 한 문장씩 표를 갱신, 조건 일치 행 수부터 센다 |
| TCL | COMMIT 확정, ROLLBACK 취소, SAVEPOINT 부분 지점 |
`,
    },
    {
      id: 'sql-ddl',
      name: 'DDL과 제약조건',
      topics: ['CREATE·ALTER·DROP', '5대 제약조건', 'ON DELETE CASCADE'],
      content: `## DDL과 제약조건

**DDL(Data Definition Language, 데이터 정의어)** 은 데이터가 아니라 **테이블의 틀(구조)** 을 만들고 고치고 없애는 명령입니다. 공책의 내용이 아니라 공책 자체와 칸 나누기를 다루는 것입니다. 대표 명령은 **CREATE, ALTER, DROP** 입니다.

### CREATE TABLE — 틀 만들기

CREATE TABLE 사원 ( 사번 INT PRIMARY KEY, 이름 VARCHAR(20) NOT NULL, 급여 INT DEFAULT 0, 부서번호 INT, FOREIGN KEY (부서번호) REFERENCES 부서(부서번호) );

- 괄호 안에 **컬럼명 자료형 제약조건** 순으로 나열합니다.
- 자료형 예: INT(정수), VARCHAR(n)(가변 길이 문자열), CHAR(n)(고정 길이), DATE(날짜).
- 기존 테이블 복제 생성: CREATE TABLE 백업 AS SELECT * FROM 사원; (AS 키워드가 빈칸 단골)

### 5대 제약조건 (Constraint) — 실기 핵심 암기

| 제약조건 | 의미 | 비유 |
|---|---|---|
| PRIMARY KEY | 기본키. 행을 유일하게 식별, NULL 불가 + 중복 불가 | 주민등록번호 |
| FOREIGN KEY | 외래키. 다른 테이블의 기본키를 참조 | 학생증의 학교 코드 |
| UNIQUE | 중복 불가 (NULL은 허용) | 닉네임 |
| NOT NULL | 빈값(NULL) 금지 | 필수 입력 칸 |
| CHECK | 조건을 만족하는 값만 허용 | 나이는 0 이상 같은 규칙 |
| DEFAULT | 값 생략 시 자동으로 넣을 기본값 | 미기재 시 0 처리 |

- **PRIMARY KEY = UNIQUE + NOT NULL** 로 정리해 두면 서술형에 그대로 씁니다. 테이블당 하나만 선언할 수 있습니다.
- **FOREIGN KEY** 는 참조하는 부모 테이블에 **없는 값을 넣을 수 없게** 막습니다. 이렇게 참조 관계가 항상 유효하도록 지키는 성질이 **참조 무결성(Referential Integrity)** 입니다. 기본키의 유일·비NULL 성질은 **개체 무결성(Entity Integrity)**, CHECK 등 값 규칙은 **도메인 무결성**입니다.
- CHECK 예: 급여 INT CHECK (급여 >= 0)

### ON DELETE 옵션 — 부모 행이 지워질 때

FOREIGN KEY (부서번호) REFERENCES 부서(부서번호) ON DELETE CASCADE 처럼 외래키 뒤에 붙입니다.

- **ON DELETE CASCADE**: 부모 행이 삭제되면 **그 값을 참조하던 자식 행도 함께 삭제**됩니다. 반이 없어지면 그 반 학생 명부도 함께 정리되는 것입니다.
- **ON DELETE SET NULL**: 자식의 외래키 값을 NULL로 바꿉니다.
- 옵션이 없으면(기본) 자식이 참조 중인 부모 행은 **삭제가 거부**됩니다.
- "부모 행 삭제 후 자식 테이블의 상태를 쓰시오" 유형에서 CASCADE 여부가 정답을 가릅니다.

### ALTER TABLE — 틀 고치기

- 컬럼 추가: ALTER TABLE 사원 **ADD** 이메일 VARCHAR(50);
- 컬럼 자료형 변경: ALTER TABLE 사원 **MODIFY** 이름 VARCHAR(30); (표준·오라클 계열, MySQL도 MODIFY)
- 컬럼 삭제: ALTER TABLE 사원 **DROP COLUMN** 이메일;
- ADD / MODIFY(또는 ALTER COLUMN) / DROP COLUMN 세 동사가 빈칸 최다 출제입니다.

### DROP TABLE — 틀 없애기

- DROP TABLE 사원; 은 데이터와 구조를 **모두 제거**합니다.
- **DROP TABLE 사원 CASCADE;** 는 이 테이블을 참조하는 객체(뷰, 외래키 제약 등)까지 **연쇄 제거**합니다. RESTRICT는 참조가 있으면 제거를 거부합니다. CASCADE/RESTRICT 구분이 단골 출제입니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| DDL 3형제 | CREATE(생성), ALTER(변경), DROP(제거) |
| 기본키 성질 | PRIMARY KEY = UNIQUE + NOT NULL, 테이블당 1개 |
| 무결성 용어 | 개체(기본키)·참조(외래키)·도메인(값 규칙) 무결성 |
| ON DELETE CASCADE | 부모 삭제 시 자식 행 연쇄 삭제 |
| ALTER 동사 | ADD·MODIFY·DROP COLUMN 빈칸 대비 |
`,
    },
    {
      id: 'sql-dcl-view',
      name: 'DCL과 뷰',
      topics: ['GRANT·REVOKE 문법', '뷰 생성과 특징', '인덱스'],
      content: `## DCL과 뷰

**DCL(Data Control Language, 데이터 제어어)** 은 "누가 무엇을 할 수 있는지" 권한을 다루는 명령입니다. 건물의 출입 카드 발급(GRANT)과 회수(REVOKE)에 해당합니다. 실기에서 **문법 빈칸으로 가장 자주 나오는 영역**입니다.

### GRANT — 권한 부여

기본형: **GRANT** 권한 **ON** 객체 **TO** 사용자;

- 예: GRANT SELECT, UPDATE ON 사원 TO kim;
- 권한 자리에는 SELECT, INSERT, UPDATE, DELETE, ALL 등이 옵니다.
- **WITH GRANT OPTION**: 받은 권한을 **다른 사람에게 다시 줄 수 있는** 권한까지 부여합니다. 열쇠와 함께 "복사 허용권"을 준 것입니다. GRANT SELECT ON 사원 TO kim WITH GRANT OPTION;
- 빈칸 출제 1순위: **ON, TO, WITH GRANT OPTION** 세 곳입니다.

### REVOKE — 권한 회수

기본형: **REVOKE** 권한 **ON** 객체 **FROM** 사용자;

- 예: REVOKE UPDATE ON 사원 FROM kim;
- GRANT는 **TO**, REVOKE는 **FROM** — 방향이 반대라는 점이 최다 함정입니다. "준다 TO, 뺏는다 FROM"으로 암기합니다.
- **CASCADE 옵션**: REVOKE ~ FROM kim CASCADE; 는 kim이 GRANT OPTION으로 **다른 사람에게 재부여한 권한까지 연쇄 회수**합니다. GRANT OPTION FOR를 붙이면 재부여 권한만 회수합니다.

### 뷰(View) — 저장된 가상 테이블

**뷰**는 SELECT문에 이름을 붙여 저장해 둔 **가상 테이블(Virtual Table)** 입니다. 데이터를 복사해 두는 것이 아니라, 조회할 때마다 원본에서 새로 가져옵니다. 창문에 비유하면, 창문 너머 풍경(원본)이 바뀌면 창문으로 보이는 모습도 바뀝니다.

- 생성: **CREATE VIEW** 영업부뷰 **AS** SELECT 이름, 급여 FROM 사원 WHERE 부서 = '영업부';
- 사용: SELECT * FROM 영업부뷰; — 일반 테이블처럼 조회합니다.
- 삭제: DROP VIEW 영업부뷰;
- CREATE VIEW ~ **AS** 의 AS가 빈칸 단골입니다.

### 뷰의 특징 (서술형 대비)

- 데이터를 **물리적으로 저장하지 않는** 논리적(가상) 테이블이다.
- 필요한 컬럼만 보여 주어 **보안**에 유리하고, 복잡한 쿼리를 단순하게 재사용할 수 있어 **편리**하다.
- 원본 테이블이 변하면 뷰의 조회 결과도 **자동으로 반영**된다.
- **뷰 정의는 ALTER로 변경할 수 없고**, DROP 후 재생성(또는 CREATE OR REPLACE)한다.
- 집계·조인 등이 포함된 뷰는 INSERT·UPDATE 같은 **갱신에 제약**이 있다.
- 뷰를 기반으로 또 다른 뷰를 만들 수 있다.

### 인덱스(Index) — 빠른 검색용 색인

**인덱스**는 책 뒤의 "찾아보기"처럼, 원하는 행을 빨리 찾기 위한 **별도의 검색 구조**입니다.

- 생성: **CREATE INDEX** idx_name **ON** 사원(이름);
- 복합 인덱스: CREATE INDEX idx_dept_sal ON 사원(부서, 급여);
- 중복 금지 겸용: CREATE UNIQUE INDEX idx_email ON 사원(이메일);
- 삭제: DROP INDEX idx_name;
- 특징: **검색(SELECT)은 빨라지지만**, 색인을 함께 갱신해야 하므로 **INSERT·UPDATE·DELETE는 느려지고** 저장 공간을 추가로 차지합니다. 장점·단점을 한 쌍으로 서술하는 문제가 나옵니다.
- 기본키를 만들면 DBMS가 자동으로 인덱스를 생성하는 것이 일반적입니다.

### 출제 포인트

| 포인트 | 내용 |
|---|---|
| GRANT 문법 | GRANT 권한 ON 객체 TO 사용자 |
| REVOKE 문법 | REVOKE 권한 ON 객체 FROM 사용자 — TO/FROM 방향 주의 |
| WITH GRANT OPTION | 권한 재부여권, REVOKE ~ CASCADE로 연쇄 회수 |
| 뷰 | CREATE VIEW ~ AS SELECT, 가상 테이블·물리 저장 없음 |
| 인덱스 | 조회 빠름 vs 갱신 느림·공간 추가 — 장단점 세트 암기 |
`,
    },
  ],
}
