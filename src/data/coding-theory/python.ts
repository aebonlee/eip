import type { TheoryArea } from '../practical-theory/types'

export const pythonCodingArea: TheoryArea = {
  id: 'python-theory',
  name: 'Python',
  icon: 'fa-brands fa-python',
  description:
    '실기 코딩 문제의 단골 언어. 슬라이싱·컬렉션 메서드·컴프리헨션·얕은 복사 함정까지, 출력 결과를 정확히 읽어내는 기초 문법을 처음부터 정리한다.',
  chapters: [
    {
      id: 'py-basic',
      name: 'Python 기초',
      topics: ['들여쓰기 문법', '동적 타이핑', 'print 함수', 'sep와 end 옵션'],
      content: `
## Python은 어떤 언어인가

Python은 문법이 사람의 말과 가장 닮은 프로그래밍 언어다. C나 Java가 중괄호와 세미콜론으로 가득한 설계 도면이라면, Python은 줄글로 쓴 요리 레시피에 가깝다. 그래서 실기 시험에서도 "이 코드의 출력 결과를 쓰시오" 유형에 가장 자주 등장한다.

### 들여쓰기(Indentation)가 곧 문법이다

- 다른 언어는 코드 묶음(블록)을 중괄호 { }로 표시하지만, Python은 **들여쓰기(Indentation)** 자체가 블록이다
- 비유하면 책의 목차와 같다. 큰 제목 아래 한 칸 들어간 항목은 그 제목에 속한 내용이라는 뜻이다
- 관례는 **공백 4칸**이며, 같은 블록 안에서는 들여쓰기 깊이가 반드시 같아야 한다
- 들여쓰기가 어긋나면 **IndentationError**가 발생한다. 실기에서 "오류가 나는 이유"를 물으면 이 용어로 답한다

### 동적 타이핑(Dynamic Typing)

- C는 int a; 처럼 상자의 종류를 미리 선언하지만, Python은 **변수 선언 시 자료형을 쓰지 않는다**
- 값을 넣는 순간 자료형이 자동으로 정해지는 방식을 **동적 타이핑(Dynamic Typing)** 이라 한다
- 같은 변수에 a = 10 을 넣었다가 a = "hello" 를 넣어도 오류가 아니다. 이름표만 다른 상자로 옮겨 붙이는 셈이다
- 자료형 확인은 type() 함수를 쓴다. print(type(10)) 의 결과는 <class 'int'> 이다

### print 함수의 동작 (실기 단골)

print는 화면에 값을 출력하는 함수다. 기본 동작 두 가지를 정확히 알아야 출력 문제를 풀 수 있다.

- 여러 값을 쉼표로 나열하면 **공백 한 칸**으로 구분해 출력한다. print("A", "B", "C") 의 결과는 A B C
- 출력이 끝나면 자동으로 **줄바꿈(개행)** 한다. print를 두 번 쓰면 두 줄이 된다

### sep와 end 옵션 (최빈출)

| 옵션 | 기본값 | 역할 | 예시 | 출력 결과 |
| --- | --- | --- | --- | --- |
| **sep** | 공백 한 칸 | 값 사이 구분 문자 | print("A", "B", sep="-") | A-B |
| **end** | 줄바꿈 | 출력 끝에 붙는 문자 | print("X", end="") 다음 줄 print("Y") | XY |

- sep는 separator(구분자), end는 마지막에 붙이는 꼬리표라고 기억하면 된다
- print("2026", "07", "17", sep="/") 의 결과는 2026/07/17
- end=""로 줄바꿈을 없애면 다음 print 출력이 **같은 줄에 이어 붙는다**. 반복문 안의 print(i, end=" ")가 한 줄로 출력되는 이유다

### 주석(Comment)

- 한 줄 주석은 # 기호를 쓴다. # 뒤의 내용은 실행되지 않는다
- 주석은 코드를 읽는 사람을 위한 메모지이며, 출력 결과에 아무 영향이 없다

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| 블록 구분 | 중괄호가 아닌 **들여쓰기**, 어긋나면 IndentationError |
| 자료형 선언 | 선언 없음, 값 대입 시 자동 결정 = **동적 타이핑** |
| print 기본 | 쉼표 나열은 공백 구분, 끝나면 자동 줄바꿈 |
| sep 옵션 | 값 사이 구분자 변경. sep="-"면 A-B |
| end 옵션 | 끝 문자 변경. end=""면 다음 출력이 같은 줄에 붙음 |
`,
    },
    {
      id: 'py-types-operators',
      name: '자료형과 연산자',
      topics: ['정수 나눗셈 //', '나머지 %', '거듭제곱 **', '문자열 곱셈과 연결', '형 변환'],
      content: `
## 숫자 자료형

Python의 기본 숫자 자료형은 두 가지다. 사과 개수처럼 딱 떨어지는 수는 **정수(int)**, 키 172.5cm처럼 소수점이 있는 수는 **실수(float)** 다.

- print(type(10)) 의 결과는 <class 'int'>
- print(type(3.14)) 의 결과는 <class 'float'>
- 참/거짓을 담는 **불(bool)** 은 True와 False 두 값뿐이며, 계산식에서는 True가 1, False가 0으로 취급된다. print(True + True) 의 결과는 2

### 나눗셈 3형제 (실기 최빈출)

나눗셈 연산자 세 개의 구분이 Python 출력 문제의 가장 큰 단골이다. 사탕 7개를 2명에게 나누는 상황으로 기억하자.

| 연산자 | 이름 | 의미 | 예시 | 결과 |
| --- | --- | --- | --- | --- |
| / | 나눗셈 | 결과가 항상 실수 | 7 / 2 | 3.5 |
| **//** | **정수 나눗셈(몫 연산자)** | 몫만 취하고 소수점 버림 | 7 // 2 | 3 |
| **%** | **나머지 연산자(모듈로)** | 나누고 남은 것 | 7 % 2 | 1 |

- 사탕 7개를 2명에게 나누면 한 명당 3개(몫, //)이고 1개가 남는다(나머지, %)
- **주의**: /는 나누어떨어져도 실수다. print(4 / 2) 의 결과는 2.0
- **음수 함정**: //는 소수점을 무조건 **작은 쪽(내림)** 으로 버린다. print(-7 // 2) 의 결과는 -3이 아니라 **-4**
- divmod(7, 2) 는 몫과 나머지를 한 번에 튜플로 준다. 결과는 (3, 1)

### 거듭제곱과 기타 연산

- **거듭제곱(**)**: 2 ** 10 의 결과는 1024. "2를 10번 곱하라"는 뜻이다
- 연산 우선순위는 수학과 같다: 괄호 > ** > 곱셈·나눗셈 > 덧셈·뺄셈
- 짝수 판정은 n % 2 == 0, 배수 판정은 n % 3 == 0 형태로 나머지 연산을 쓴다 (반복문 문제의 단골 조건)

### 문자열 연결과 곱셈 (최빈출)

문자열에도 +와 *를 쓸 수 있는데, 의미가 숫자와 완전히 다르다.

- **연결(+)**: 문자열끼리 이어 붙인다. print("Hi" + "gh") 의 결과는 High
- **반복(*)**: 문자열을 정수만큼 복사해 붙인다. print("ab" * 3) 의 결과는 ababab
- 도장에 비유하면 +는 도장 두 개를 나란히 찍는 것, *는 같은 도장을 여러 번 찍는 것이다

### 형 변환(Type Casting) 함정

- 숫자와 문자열은 +로 바로 연결할 수 없다. "10" + 5 는 **TypeError**
- **int()**: 문자열이나 실수를 정수로. print(int("10") + 5) 의 결과는 15, print(int(3.9)) 의 결과는 3 (**반올림이 아니라 버림**)
- **str()**: 숫자를 문자열로. print(str(10) + "5") 의 결과는 105 (숫자 계산이 아니라 글자 연결)
- round()는 반올림 함수지만 **round(2.5)의 결과는 2** 다 (짝수 쪽으로 반올림하는 은행가 반올림). round(3.5)는 4

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| 7 / 2, 7 // 2, 7 % 2 | 각각 3.5, 3, 1 — 세 값 구분이 최빈출 |
| -7 // 2 | 내림이므로 **-4** (0 쪽 절삭 아님) |
| 2 ** 10 | 1024, **는 거듭제곱 |
| "ab" * 3 | ababab, 문자열 반복 |
| int("10") + 5 와 str(10) + "5" | 15 와 105 — 형 변환 방향 구분 |
| int(3.9) | 3, int()는 버림 |
`,
    },
    {
      id: 'py-slicing',
      name: '문자열 인덱싱과 슬라이싱',
      topics: ['인덱스 0 시작', '음수 인덱스', '[start:end:step]', '역순 [::-1]'],
      content: `
## 인덱싱(Indexing): 한 글자 집어내기

문자열은 글자들이 순서대로 앉아 있는 **좌석표**다. 좌석 번호가 **인덱스(Index)** 이며, 반드시 **0번부터** 시작한다.

- s = "PYTHON" 일 때 P는 0번, Y는 1번, T는 2번, H는 3번, O는 4번, N은 5번 좌석이다
- print(s[0]) 의 결과는 P, print(s[3]) 의 결과는 H
- 글자 수는 len(s) 로 구한다. print(len(s)) 의 결과는 6. **마지막 인덱스는 len - 1** 이다

### 음수 인덱스: 뒤에서 세기

- Python만의 특징으로, **음수 인덱스(Negative Index)** 는 뒤에서부터 센다. -1이 맨 뒤다
- print(s[-1]) 의 결과는 N, print(s[-2]) 의 결과는 O
- 버스 맨 뒷자리부터 -1, -2로 거꾸로 번호를 붙였다고 생각하면 된다

## 슬라이싱(Slicing): 구간 잘라내기 (실기 최빈출)

**슬라이싱(Slicing)** 은 문자열을 식빵처럼 칼로 잘라 일부 구간을 얻는 문법이다. 형식은 **[start:end:step]** 이다.

- **start**: 시작 인덱스 (포함)
- **end**: 끝 인덱스 (**포함하지 않음** — 여기가 최대 함정)
- **step**: 건너뛰는 간격 (생략하면 1)

s = "PYTHON" 기준으로 확인한다.

| 표현 | 의미 | 결과 |
| --- | --- | --- |
| s[1:4] | 1번부터 4번 **직전**까지 | YTH |
| s[:3] | 처음부터 3번 직전까지 | PYT |
| s[3:] | 3번부터 끝까지 | HON |
| s[-3:] | 뒤에서 3글자 | HON |
| s[::2] | 처음부터 끝까지 2칸씩 | PTO |
| s[1:6:2] | 1번부터 2칸씩 | YHN |
| **s[::-1]** | **전체 역순** | NOHTYP |

- end가 "직전까지"인 이유는 자로 길이를 잴 때 눈금 1에서 4까지가 3칸인 것과 같다. **글자 수 = end - start**
- 범위를 넘어가도 슬라이싱은 오류가 나지 않고 있는 만큼만 잘라 준다 (인덱싱 s[10]은 IndexError)

### 역순 [::-1] (최빈출)

- step이 음수면 **거꾸로 걷는다**. s[::-1] 은 문자열 전체를 뒤집는 관용 표현이다
- print("LEVEL"[::-1]) 처럼 원본과 역순이 같으면 **회문(Palindrome)** 판별에 쓰인다

### 리스트에도 똑같이 적용

슬라이싱 규칙은 문자열뿐 아니라 리스트에도 동일하다. a = [10, 20, 30, 40, 50] 일 때,

- print(a[1:3]) 의 결과는 [20, 30]
- print(a[::-1]) 의 결과는 [50, 40, 30, 20, 10]

### 불변(Immutable) 주의

- 문자열은 **불변(Immutable)** 이라 s[0] = "J" 처럼 한 글자만 바꾸려 하면 **TypeError** 가 발생한다
- 바꾸려면 replace나 슬라이싱 조합으로 **새 문자열을 만들어야** 한다

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| 인덱스 시작 | 0부터, 마지막은 len - 1 |
| 음수 인덱스 | -1이 맨 뒤 글자 |
| [start:end] | start 포함, **end 미포함**, 글자 수는 end - start |
| s[::-1] | 전체 역순 (회문 판별 단골) |
| s[::2] | 짝수 번째 자리만 추출 |
| 문자열 불변성 | s[0] = "J" 는 TypeError |
`,
    },
    {
      id: 'py-string-methods',
      name: '문자열 메서드',
      topics: ['upper/lower', 'strip', 'replace', 'split과 join', 'find와 count'],
      content: `
## 메서드(Method)란

**메서드(Method)** 는 자료형이 자체적으로 갖고 있는 기능 버튼이다. 문자열이라는 리모컨에 붙어 있는 버튼들을 값.버튼() 형태로 누른다고 생각하면 된다.

**중요한 대전제**: 문자열은 불변이므로 문자열 메서드는 **원본을 바꾸지 않고 새 문자열을 돌려준다**. 원본을 다시 출력하면 그대로다.

### 주요 문자열 메서드 동작 표 (실기 단골)

t = " Hello World " 와 "banana" 기준으로 정리한다.

| 메서드 | 역할 | 예시 | 결과 |
| --- | --- | --- | --- |
| **upper()** | 전부 대문자로 | "abc".upper() | ABC |
| **lower()** | 전부 소문자로 | "ABC".lower() | abc |
| **strip()** | 양쪽 끝 공백 제거 | t.strip() | Hello World |
| **replace(a, b)** | a를 모두 b로 교체 | "banana".replace("a", "o") | bonono |
| **split(구분자)** | 구분자로 잘라 리스트로 | "a,b,c".split(",") | ['a', 'b', 'c'] |
| **join(리스트)** | 리스트를 이어 문자열로 | "-".join(["a", "b", "c"]) | a-b-c |
| **find(x)** | x가 처음 나오는 인덱스 | "banana".find("na") | 2 |
| **count(x)** | x가 나오는 횟수 | "banana".count("a") | 3 |

### 하나씩 비유로 이해하기

- **strip**: 선물 포장의 양쪽 끝 테이프만 떼는 것. 가운데 공백은 그대로다. " a b ".strip() 의 결과는 a b
- **replace**: 문서 편집기의 "모두 바꾸기" 버튼. **전부** 바꾼다는 점이 핵심이다
- **split**: 김밥을 칼로 써는 것. 구분자를 기준으로 잘라 **리스트**를 만든다. 구분자를 생략한 split()은 공백 기준으로 자른다. "Hello World".split() 의 결과는 ['Hello', 'World']
- **join**: split의 반대로, 썰어 놓은 조각을 접착제(호출한 문자열)로 다시 붙인다. **접착제가 앞에, 리스트가 괄호 안에** 온다는 형태를 기억하자

### find와 index의 차이 (함정 단골)

- **find**는 찾는 값이 없으면 **-1**을 돌려준다. "banana".find("z") 의 결과는 -1
- **index**는 같은 일을 하지만 없으면 **ValueError** 오류를 일으킨다. "apple".index("p") 의 결과는 1
- "오류 없이 실패를 확인하고 싶으면 find, 반드시 있어야 하는 값이면 index"로 구분한다

### 판별 메서드 (is 계열)

- **isdigit()**: 전부 숫자인지. "123".isdigit() 의 결과는 True
- **isalpha()**: 전부 글자인지. "Hello".isalpha() 의 결과는 True
- **capitalize()**: 첫 글자만 대문자로. "hello".capitalize() 의 결과는 Hello
- **title()**: 각 단어의 첫 글자를 대문자로. "hello world".title() 의 결과는 Hello World

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| 메서드의 반환 | 원본 불변, **새 문자열 반환** (원본 재출력 시 그대로) |
| replace | 해당 문자를 **모두** 교체. banana → bonono |
| split / join | 문자열↔리스트 상호 변환 짝꿍. join은 구분자.join(리스트) 형태 |
| find 실패 시 | **-1** 반환 (index는 ValueError) |
| count | 겹치지 않게 세어 횟수 반환. "banana".count("a")는 3 |
`,
    },
    {
      id: 'py-list',
      name: '리스트',
      topics: ['append/insert/remove/pop', 'sort/reverse', 'extend', '리스트 슬라이싱'],
      content: `
## 리스트(List)란

**리스트(List)** 는 여러 값을 순서대로 담는 **칸막이 있는 필통**이다. 대괄호 [ ]로 만들고, 어떤 자료형이든 섞어 담을 수 있으며, 문자열과 달리 **변경 가능(Mutable)** 하다. 칸의 내용물을 마음대로 빼고 넣을 수 있다는 뜻이다.

- a = [1, 2, 3] 처럼 만들고, a[0] = 9 처럼 특정 칸을 직접 바꿀 수 있다
- 인덱싱·슬라이싱 규칙은 문자열과 동일하다

### 주요 리스트 메서드 동작 표 (실기 최빈출)

a = [1, 2, 3] 에서 출발해 순서대로 실행한 결과다.

| 메서드 | 역할 | 예시 | 실행 후 a |
| --- | --- | --- | --- |
| **append(x)** | 맨 뒤에 x 추가 | a.append(4) | [1, 2, 3, 4] |
| **insert(i, x)** | i번 자리에 x 끼워 넣기 | a.insert(1, 10) | [1, 10, 2, 3, 4] |
| **remove(x)** | **값** x를 찾아 첫 번째 것 삭제 | a.remove(10) | [1, 2, 3, 4] |
| **pop()** | 맨 뒤 요소를 **꺼내서 반환** | x = a.pop() 이면 x는 4 | [1, 2, 3] |
| **pop(i)** | i번 요소를 꺼내서 반환 | y = a.pop(0) 이면 y는 1 | [2, 3] |

- **append와 insert**: append는 줄 맨 뒤에 서는 것, insert는 중간에 새치기하는 것
- **remove와 pop의 차이**: remove는 **값**으로 지목해 버리고, pop은 **자리 번호**로 꺼내며 꺼낸 값을 돌려준다 (인형뽑기처럼 손에 쥐어 준다)

### 정렬과 뒤집기

b = [3, 1, 2] 기준이다.

| 메서드 | 역할 | 예시 | 실행 후 b |
| --- | --- | --- | --- |
| **sort()** | 오름차순 정렬 (원본 변경) | b.sort() | [1, 2, 3] |
| **sort(reverse=True)** | 내림차순 정렬 | b.sort(reverse=True) | [3, 2, 1] |
| **reverse()** | 현재 순서를 그대로 뒤집기 | b.reverse() | [1, 2, 3] |
| **extend(리스트)** | 다른 리스트를 낱개로 이어 붙이기 | b.extend([7, 8]) | [1, 2, 3, 7, 8] |

- **sort()와 sorted()의 차이**: b.sort()는 **원본을 바꾸고 None을 반환**, sorted(b)는 **원본은 두고 정렬된 새 리스트를 반환**한다. print(b.sort()) 의 결과가 None인 것이 함정 단골이다
- **reverse는 정렬이 아니다**: 크기와 무관하게 현재 순서만 거꾸로 뒤집는다
- **append vs extend 함정**: c = [1, 2] 에 c.append([3, 4]) 하면 리스트 통째로 한 칸에 들어가 [1, 2, [3, 4]] 가 되고, extend([3, 4]) 하면 낱개로 풀려 [1, 2, 3, 4] 가 된다

### 리스트 슬라이싱과 연산

- d = [10, 20, 30, 40] 일 때 print(d[1:3]) 의 결과는 [20, 30]
- 슬라이싱에 대입하면 구간이 통째로 바뀐다. d[0:2] = [99] 실행 후 d는 [99, 30, 40]
- [1, 2] + [3] 의 결과는 [1, 2, 3] (연결), [0] * 3 의 결과는 [0, 0, 0] (반복)
- 내장 함수: max([5, 2, 9])는 9, min은 2, sum은 16, len([1, 2, 3])은 3

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| remove vs pop | remove는 **값**으로 삭제, pop은 **인덱스**로 꺼내 **반환** |
| sort vs sorted | sort는 원본 변경·**None 반환**, sorted는 새 리스트 반환 |
| sort vs reverse | sort는 크기순 정렬, reverse는 단순 뒤집기 |
| append vs extend | append([3,4])는 통째로 한 칸, extend는 낱개로 붙임 |
| 슬라이싱 대입 | d[0:2] = [99] 는 구간 교체 |
`,
    },
    {
      id: 'py-tuple',
      name: '튜플과 언패킹',
      topics: ['불변성', '다중 할당', 'swap 한 줄 표현', '요소 1개 튜플'],
      content: `
## 튜플(Tuple)이란

**튜플(Tuple)** 은 리스트와 똑같이 여러 값을 순서대로 담지만, 한 번 만들면 **절대 바꿀 수 없는(불변, Immutable)** 자료형이다. 소괄호 ( )로 만든다.

- 리스트가 지우고 다시 쓸 수 있는 화이트보드라면, 튜플은 돌에 새긴 비석이다
- t = (1, 2, 3) 일 때 print(t[0], t[-1]) 의 결과는 1 3 (인덱싱·슬라이싱은 리스트와 동일)
- t[0] = 9 처럼 수정을 시도하면 **TypeError** 가 발생한다 — "튜플은 왜 오류가 나는가"의 답은 **불변성** 이다
- append, remove 같은 변경 메서드 자체가 없다. 있는 것은 **count()와 index()** 뿐이다. t.count(2)는 1, t.index(3)은 2

### 튜플 만들기의 함정 두 가지

- **괄호 생략 가능**: t2 = 1, 2 라고만 써도 튜플이다. print(t2) 의 결과는 (1, 2)
- **요소 1개 튜플은 쉼표 필수**: (5)는 그냥 정수 5다. 튜플로 만들려면 **(5,)** 처럼 쉼표를 붙여야 한다. print(type((5,))) 의 결과는 <class 'tuple'>
- 튜플끼리 +로 연결하면 새 튜플이 생긴다. (1, 2, 3) + (4,) 의 결과는 (1, 2, 3, 4) — 원본 수정이 아니라 새로 만드는 것이므로 불변성과 모순되지 않는다

## 언패킹(Unpacking): 포장 풀기

**언패킹(Unpacking)** 은 묶여 있는 값들을 여러 변수에 한 번에 나누어 담는 문법이다. 택배 상자를 열어 내용물을 각자 자리에 놓는 것과 같다.

- x, y, z = (1, 2, 3) 실행 후 print(x, y, z) 의 결과는 1 2 3
- 변수 개수와 값 개수가 다르면 ValueError가 발생한다

### 다중 할당(Multiple Assignment)

- a, b = 10, 20 은 사실 오른쪽이 튜플 (10, 20)으로 묶였다가 풀리는 언패킹이다
- print(a, b) 의 결과는 10 20

### swap 한 줄 표현 (실기 단골)

두 변수의 값을 맞바꾸는(swap) 코드가 자주 출제된다.

- 다른 언어는 임시 변수(temp)를 거쳐 세 줄이 필요하지만, Python은 **a, b = b, a** 한 줄이면 된다
- a, b = 10, 20 상태에서 a, b = b, a 실행 후 print(a, b) 의 결과는 **20 10**
- 원리: 오른쪽 (b, a) 즉 (20, 10)이 **먼저 튜플로 만들어진 뒤** 왼쪽에 언패킹되므로 값이 엉키지 않는다

### 함수의 다중 반환도 튜플이다

- 함수에서 return 1, 2 라고 쓰면 실제로는 **튜플 (1, 2) 하나**가 반환된다
- 받은 쪽에서 r = f() 하면 print(r, type(r)) 의 결과는 (1, 2) <class 'tuple'> 이고, a, b = f() 하면 언패킹되어 나뉜다

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| 튜플의 성질 | **불변(Immutable)**, t[0] = 9 는 TypeError |
| 사용 가능 메서드 | count, index 두 개뿐 (append 없음) |
| 요소 1개 튜플 | (5,) 처럼 **쉼표 필수**, (5)는 정수 |
| swap 한 줄 | a, b = b, a → 10, 20이 20 10으로 |
| return 1, 2 | 튜플 (1, 2) 하나로 반환됨 |
`,
    },
    {
      id: 'py-dict',
      name: '딕셔너리',
      topics: ['키-값 쌍', 'keys/values/items', 'get 메서드', 'in 판정', '딕셔너리 순회'],
      content: `
## 딕셔너리(Dictionary)란

**딕셔너리(Dictionary)** 는 이름 그대로 사전이다. 사전에서 단어(키)를 찾으면 뜻(값)이 나오듯, **키(Key)와 값(Value)을 쌍**으로 저장한다. 중괄호 { }와 콜론으로 만든다.

- d = {"apple": 500, "banana": 300} 은 "apple이라는 키에 500이라는 값"을 담은 것이다
- 리스트가 좌석 번호(인덱스)로 찾는 지정석이라면, 딕셔너리는 **이름표로 찾는 사물함**이다
- 값 꺼내기: print(d["apple"]) 의 결과는 500
- 값 추가·수정: d["cherry"] = 1000 처럼 대입하면, **키가 없으면 추가되고 있으면 수정**된다
- 삭제: del d["apple"] 로 키-값 쌍을 지운다
- **키는 중복 불가**, 값은 중복 가능. 같은 키로 두 번 대입하면 마지막 값으로 덮어써진다

### 없는 키 접근: 대괄호 vs get (함정 단골)

- d["melon"] 처럼 없는 키를 대괄호로 찾으면 **KeyError** 가 발생한다
- **get()** 메서드는 오류 대신 **None**을 돌려준다. print(d.get("melon")) 의 결과는 None
- get의 두 번째 인자는 기본값이다. print(d.get("melon", 0)) 의 결과는 0
- "안전하게 조회하려면 get"이 답안 키워드다

### keys / values / items 3형제

d = {"apple": 500, "banana": 300, "cherry": 1000} 기준이다.

| 메서드 | 역할 | list()로 감싼 결과 |
| --- | --- | --- |
| **keys()** | 키만 모아서 | ['apple', 'banana', 'cherry'] |
| **values()** | 값만 모아서 | [500, 300, 1000] |
| **items()** | (키, 값) 튜플 쌍으로 | [('apple', 500), ('banana', 300), ('cherry', 1000)] |

### in 판정의 기준은 키다 (최빈출 함정)

- 딕셔너리에 in 연산자를 쓰면 **키를 기준으로만** 검사한다
- print("apple" in d) 의 결과는 True
- print(500 in d) 의 결과는 **False** — 500은 값이지 키가 아니기 때문이다
- 값을 검사하려면 500 in d.values() 라고 명시해야 한다

### 딕셔너리 순회 (반복문 결합 단골)

- for k in d: 라고 쓰면 **키만** 차례로 나온다. for k in {"a": 1, "b": 2}: print(k) 의 출력은 a 와 b 두 줄
- 키와 값을 함께 돌리려면 items()와 언패킹을 쓴다
- for k, v in {"a": 1, "b": 2}.items(): print(k, v) 의 출력은 a 1 과 b 2 두 줄
- len(d) 는 키-값 **쌍의 개수**다. len({"a": 1, "b": 2}) 의 결과는 2

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| 구조 | **키-값 쌍** 저장, 키는 중복 불가(덮어쓰기) |
| 없는 키 조회 | d[키]는 **KeyError**, d.get(키)는 **None**(기본값 지정 가능) |
| in 연산자 | **키 기준** 검사. 값 검사는 d.values()에 in |
| items() 순회 | for k, v in d.items(): 로 키·값 동시 언패킹 |
| d[키] = 값 | 없으면 추가, 있으면 수정 |
`,
    },
    {
      id: 'py-set',
      name: '집합',
      topics: ['중복 제거', '합집합 |', '교집합 &', '차집합 -', '대칭차 ^'],
      content: `
## 집합(Set)이란

**집합(Set)** 은 수학 시간의 집합을 그대로 옮긴 자료형이다. 두 가지 성질이 전부다: **중복을 허용하지 않고, 순서가 없다**.

- 뽑기 통에 같은 색 구슬을 아무리 넣어도 "들어 있는 색의 종류"는 하나로 세는 것과 같다
- 중괄호 { }로 만들되, 딕셔너리와 달리 콜론 없이 값만 나열한다
- **빈 집합은 set()으로만** 만든다. {}는 빈 딕셔너리다 (함정 단골)
- 순서가 없으므로 인덱싱 s[0]이 불가능하다

### 중복 제거 (실기 최빈출)

- s = {1, 2, 2, 3, 3, 3} 을 출력하면 {1, 2, 3} — 만들어지는 순간 중복이 사라진다
- **set() 변환이 중복 제거의 관용 표현**이다. print(set([1, 1, 2])) 의 결과는 {1, 2}
- 문자열도 글자 단위로 분해된다. print(len(set("banana"))) 의 결과는 3 (b, a, n 세 종류)
- 순서가 필요하면 sorted로 마무리한다. print(sorted(set([3, 1, 3, 2]))) 의 결과는 [1, 2, 3] (sorted는 리스트를 반환)

### 집합 연산자 4종 (실기 최빈출)

a = {1, 2, 3}, b = {3, 4, 5} 기준이다.

| 연산자 | 이름 | 의미 | 결과 |
| --- | --- | --- | --- |
| **a &#124; b** | **합집합(Union)** | 양쪽 전부 합치기 | {1, 2, 3, 4, 5} |
| **a & b** | **교집합(Intersection)** | 양쪽에 공통인 것 | {3} |
| **a - b** | **차집합(Difference)** | a에만 있는 것 | {1, 2} |
| **a ^ b** | **대칭차집합(Symmetric Difference)** | 한쪽에만 있는 것 | {1, 2, 4, 5} |

- 비유: 두 반의 방과후 신청자 명단에서, 합집합은 전체 명단, 교집합은 둘 다 신청한 학생, 차집합은 우리 반만 신청한 학생, 대칭차는 한 반에서만 신청한 학생이다
- 메서드 표기도 같은 뜻이다: union(), intersection(), difference() — 답안에 연산자·메서드 어느 쪽을 요구하는지 문제를 확인한다
- 차집합은 **방향이 있다**. a - b와 b - a는 다르다 (b - a의 결과는 {4, 5})

### 요소 추가와 삭제

- **add(x)**: 하나 추가. a.add(9) 후 a는 {1, 2, 3, 9} (리스트의 append와 이름이 다른 점 주의)
- **remove(x)**: 삭제하되 없으면 KeyError. **discard(x)** 는 없어도 조용히 넘어간다
- in 연산자로 소속 검사를 하면 리스트보다 빠르다. print(2 in {1, 2, 3}) 의 결과는 True

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| 집합의 두 성질 | **중복 불허 + 순서 없음** (인덱싱 불가) |
| 중복 제거 관용구 | set(리스트), 정렬까지 하려면 sorted(set(리스트)) |
| 연산자 4종 | 합집합 &#124;, 교집합 &, 차집합 -, 대칭차 ^ |
| 빈 집합 | set() — {}는 빈 **딕셔너리** |
| 추가 메서드 | add (append 아님), 안전 삭제는 discard |
`,
    },
    {
      id: 'py-control',
      name: '조건문과 반복문',
      topics: ['if-elif-else', 'range 3인자', 'while', 'break와 continue', 'for-else', 'enumerate'],
      content: `
## 조건문: if - elif - else

조건문은 갈림길 표지판이다. 조건을 위에서부터 검사해 **처음 참이 되는 길 하나만** 지나간다.

- **elif** 는 else if의 줄임말로, 앞 조건이 거짓일 때만 검사하는 두 번째 갈림길이다
- x = 85 일 때, if x >= 90: print("A") / elif x >= 80: print("B") / else: print("C") 의 출력은 **B**
- 85는 90 이상도 맞지 않고, 80 이상 조건에서 처음 참이 되어 B가 출력되고 **아래는 검사조차 하지 않는다**
- 조건식에는 비교 연산자(==, !=, >=)와 논리 연산자 **and, or, not** 을 조합한다 (&&, || 아님 — 언어 구분 단골)

## 반복문 for와 range

**range(시작, 끝, 간격)** 는 숫자 줄넘기 카운터다. 슬라이싱과 똑같이 **끝은 포함하지 않는다**.

| 표현 | 의미 | list()로 본 결과 |
| --- | --- | --- |
| range(5) | 0부터 5 직전까지 | [0, 1, 2, 3, 4] |
| range(1, 10, 2) | 1부터 2씩 건너뛰며 | [1, 3, 5, 7, 9] |
| **range(10, 0, -2)** | 10부터 거꾸로 2씩 | [10, 8, 6, 4, 2] |

- 1부터 10까지 합: total = 0 에서 for i in range(1, 11): total += i 실행 후 print(total) 의 결과는 55. **range(1, 10)이 아니라 range(1, 11)** 이어야 10이 포함된다
- **while** 은 조건이 참인 동안 반복한다. i = 0 에서 while i < 3: print(i, end=" ") 후 i += 1 을 돌리면 출력은 0 1 2 — 조건 갱신을 빠뜨리면 **무한 루프**가 된다

### break와 continue

- **break**: 반복문을 그 자리에서 완전히 탈출 (경기 중도 포기)
- **continue**: 이번 회차만 건너뛰고 다음 회차로 (한 바퀴 패스)
- for i in range(5): 안에서 if i == 2: continue 후 print(i, end=" ") 의 출력은 0 1 3 4 — 2만 빠진다

### for-else (Python만의 특이 문법, 함정 단골)

- 반복문 뒤의 **else 블록은 break 없이 반복이 끝까지 돌았을 때만 실행**된다
- "탈옥(break) 없이 형기를 마쳐야 받는 만기 출소 도장"으로 기억하자
- for i in range(5): 안에서 if i == 3: break 가 실행되면 else의 print는 **출력되지 않는다**
- break 조건이 한 번도 참이 되지 않으면 else 블록이 실행된다 — 검색 실패 처리에 자주 쓰인다

### enumerate (번호표 붙이기)

- **enumerate(반복대상)** 는 요소에 **번호표(인덱스)를 붙여 (번호, 값) 쌍**을 만들어 준다
- for i, ch in enumerate("abc"): print(i, ch) 의 출력은 0 a, 1 b, 2 c 세 줄
- 시작 번호를 바꿀 수 있다. enumerate("abc", 1) 이면 1 a, 2 b, 3 c — 두 번째 인자가 시작값이다

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| elif | 위에서부터 검사, **처음 참인 블록 하나만** 실행 |
| range(1, 11) | 끝 미포함이므로 1~10. range(10, 0, -2)는 10 8 6 4 2 |
| break vs continue | 완전 탈출 vs 이번 회차만 건너뜀 |
| for-else | **break 없이 정상 종료해야만** else 실행 |
| enumerate | (인덱스, 값) 쌍 생성, 둘째 인자로 시작 번호 지정 |
`,
    },
    {
      id: 'py-function',
      name: '함수',
      topics: ['def와 return', '기본 인자', '가변 인자 *args', '다중 반환', 'lambda'],
      content: `
## 함수(Function)란

**함수(Function)** 는 재료를 넣으면 결과가 나오는 자판기다. 코드에 이름을 붙여 두고 필요할 때마다 호출해 재사용한다.

- **def** 키워드로 정의하고, **매개변수(Parameter)** 로 재료를 받고, **return** 으로 결과를 돌려준다
- def add(a, b): return a + b 를 정의하면 print(add(3, 5)) 의 결과는 8
- 호출할 때 실제로 넣는 값 3, 5는 **인자(Argument)** 라고 부른다 — 매개변수와 인자의 용어 구분도 출제된다
- return을 만나면 함수는 **그 즉시 종료**된다. return 뒤의 코드는 실행되지 않는다
- **return이 없는 함수는 None을 반환**한다. print 함수 자체도 반환값이 없어서, print(print("hi")) 의 출력은 hi 다음 줄에 **None** 이다 (함정 단골)

### 기본 인자(Default Argument)

- 매개변수에 미리 값을 정해 두면, 인자를 생략했을 때 그 값이 쓰인다
- def greet(name, msg="안녕"): return msg + " " + name 일 때
- print(greet("철수")) 의 결과는 안녕 철수, print(greet("철수", "반가워")) 의 결과는 반가워 철수
- 기본 인자는 **기본 인자가 없는 매개변수보다 뒤에** 와야 한다 (앞에 오면 SyntaxError)

### 가변 인자 *args (실기 단골)

- 인자를 몇 개 받을지 미리 알 수 없을 때 매개변수 앞에 별표를 붙인다: def total(*args):
- 들어온 인자들이 **튜플로 묶여** args에 담긴다. 손님이 몇 명이 오든 다 태우는 대형 버스라고 생각하면 된다
- def total(*args): return sum(args) 일 때 print(total(1, 2, 3)) 의 결과는 6, print(total(10, 20)) 의 결과는 30
- 참고로 **키워드 가변 인자 kwargs** 는 별표 두 개를 붙여 딕셔너리로 받는다

### 다중 반환 (튜플 반환)

- def calc(a, b): return a + b, a - b 처럼 쉼표로 나열하면 **튜플 하나로 묶여** 반환된다
- s, d = calc(10, 3) 으로 언패킹해 받으면 print(s, d) 의 결과는 13 7

### lambda (이름 없는 한 줄 함수)

- **lambda 매개변수: 식** 형태의 즉석 일회용 함수다. 정식 주방(def) 대신 간이 포장마차라고 보면 된다
- f = lambda x: x * 2 일 때 print(f(5)) 의 결과는 10
- add = lambda a, b: a + b 일 때 print(add(3, 4)) 의 결과는 7
- return을 쓰지 않아도 **식의 결과가 자동 반환**된다. sorted의 key, map, filter의 인자로 자주 쓰인다

### 변수의 범위(Scope)

- 함수 안에서 만든 변수는 **지역 변수(Local Variable)** 로, 함수 밖에서는 보이지 않는다
- 함수 안에서 바깥 **전역 변수(Global Variable)** 를 수정하려면 **global** 선언이 필요하다. global 없이 대입하면 새 지역 변수가 생길 뿐이다

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| return 없는 함수 | **None** 반환. print(print("hi"))는 hi와 None 출력 |
| 기본 인자 | 생략 시 기본값 사용, 비기본 매개변수보다 뒤에 위치 |
| *args | 여러 인자를 **튜플**로 수집 |
| return a, b | 튜플 반환 → 언패킹으로 받기 |
| lambda | lambda x: x * 2 — 한 줄 무명 함수, 식 결과 자동 반환 |
| global | 함수 안에서 전역 변수를 수정할 때 필요 |
`,
    },
    {
      id: 'py-comprehension',
      name: '컴프리헨션과 내장함수',
      topics: ['리스트 컴프리헨션', '조건부 컴프리헨션', 'map/filter', 'zip', 'sorted key'],
      content: `
## 리스트 컴프리헨션(List Comprehension)

**리스트 컴프리헨션** 은 반복문으로 리스트를 만드는 3~4줄짜리 코드를 **한 줄로 압축한 문법**이다. 공장 컨베이어 벨트에 "각 재료를 이렇게 가공해서 상자에 담아라"라고 한 문장으로 지시하는 것과 같다.

- 형식: **[식 for 변수 in 반복대상]**
- print([i * 2 for i in range(5)]) 의 결과는 [0, 2, 4, 6, 8]
- print([i * i for i in range(1, 6)]) 의 결과는 [1, 4, 9, 16, 25]

### 조건부 컴프리헨션 (실기 단골)

- 뒤에 if를 붙이면 **조건에 맞는 것만 골라** 담는다: [식 for 변수 in 반복대상 if 조건]
- print([i for i in range(10) if i % 2 == 0]) 의 결과는 [0, 2, 4, 6, 8] — 짝수만 통과하는 검문소다
- 중괄호로 감싸면 딕셔너리 컴프리헨션이 된다. print({i: i * i for i in range(3)}) 의 결과는 {0: 0, 1: 1, 2: 4}

## map과 filter (일괄 변환과 선별)

| 함수 | 역할 | 비유 |
| --- | --- | --- |
| **map(함수, 반복대상)** | 모든 요소에 함수를 **일괄 적용** | 전 직원에게 같은 유니폼 입히기 |
| **filter(함수, 반복대상)** | 함수 결과가 참인 요소만 **선별** | 체로 걸러내기 |

- 둘 다 결과를 바로 보여주지 않는 게으른 객체라서 **list()로 감싸야 내용이 보인다** (함정 단골)
- print(list(map(int, ["1", "2", "3"]))) 의 결과는 [1, 2, 3] — 문자열 리스트를 숫자로 일괄 변환하는 최빈출 관용구
- print(list(map(lambda x: x + 1, [1, 2, 3]))) 의 결과는 [2, 3, 4]
- print(list(filter(lambda x: x > 2, [1, 2, 3, 4]))) 의 결과는 [3, 4]

## zip (지퍼처럼 짝짓기)

- **zip** 은 여러 반복대상을 지퍼 잠그듯 **같은 위치끼리 튜플로 묶는다**
- print(list(zip([1, 2, 3], ["a", "b", "c"]))) 의 결과는 [(1, 'a'), (2, 'b'), (3, 'c')]
- 문자열도 묶인다. print(list(zip("ab", "xy"))) 의 결과는 [('a', 'x'), ('b', 'y')]
- 길이가 다르면 **짧은 쪽에 맞춰** 잘린다

## sorted와 key 옵션 (실기 단골)

- **sorted(반복대상)** 는 원본은 그대로 두고 **정렬된 새 리스트**를 반환한다
- **key 옵션**은 "무엇을 기준으로 줄 세울지"를 정하는 심사 기준표다
- print(sorted(["banana", "kiwi", "apple"], key=len)) 의 결과는 ['kiwi', 'apple', 'banana'] — 글자 수(4, 5, 6) 기준 정렬
- print(sorted([("b", 2), ("a", 1)], key=lambda x: x[1])) 의 결과는 [('a', 1), ('b', 2)] — 각 튜플의 1번 요소 기준
- reverse=True를 함께 주면 내림차순이다. sorted([3, 1, 2], reverse=True) 의 결과는 [3, 2, 1]

### 자주 나오는 내장함수(Built-in Function) 요약

| 함수 | 역할 | 예시와 결과 |
| --- | --- | --- |
| len | 길이(개수) | len("hello")는 5 |
| sum | 합계 | sum([1, 2, 3])은 6 |
| max / min | 최댓값 / 최솟값 | max("abc")는 c |
| abs | 절댓값 | abs(-5)는 5 |

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| 컴프리헨션 형식 | [식 for 변수 in 대상 if 조건] — if는 맨 뒤 |
| map / filter | 일괄 변환 / 조건 선별, 출력 시 **list() 필수** |
| map(int, 리스트) | 문자열 리스트 → 정수 리스트 관용구 |
| zip | 같은 위치끼리 튜플로, 짧은 쪽 길이에 맞춤 |
| sorted key=len | 길이 기준 정렬, key=lambda x: x[1]은 1번 요소 기준 |
`,
    },
    {
      id: 'py-class-copy',
      name: '클래스와 얕은 복사',
      topics: ['class와 __init__', 'self', '상속과 오버라이딩', '얕은 복사 vs 깊은 복사'],
      content: `
## 클래스(Class)란

**클래스(Class)** 는 붕어빵 틀이고, 그 틀로 찍어낸 붕어빵 하나하나가 **객체(Object) 또는 인스턴스(Instance)** 다. 틀 하나로 팥 붕어빵, 슈크림 붕어빵을 여러 개 만들 수 있다.

- 객체가 가진 데이터를 **속성(Attribute)**, 객체가 할 수 있는 동작을 **메서드(Method)** 라고 한다

### __init__과 self

- **__init__** 은 객체가 만들어지는 순간 자동 실행되는 **생성자(Constructor)** 다. 붕어빵을 찍는 순간 속을 채워 넣는 과정이다
- **self** 는 "지금 만들어지고 있는 그 객체 자신"을 가리키며, **모든 메서드의 첫 번째 매개변수**로 반드시 쓴다
- class Person: 안에 def __init__(self, name): self.name = name 을 정의하고 p = Person("영희") 하면 print(p.name) 의 결과는 영희
- 메서드 def hello(self): return "안녕, " + self.name 을 추가하면 print(p.hello()) 의 결과는 안녕, 영희
- 호출할 때 self는 쓰지 않는다. p.hello() 라고 하면 Python이 p를 self 자리에 자동으로 넣는다

### 상속(Inheritance)과 오버라이딩(Overriding)

- **상속** 은 부모 클래스의 속성·메서드를 자식이 물려받는 것이다. class Student(Person): 처럼 **괄호 안에 부모 이름**을 쓴다
- **super().__init__(name)** 은 부모의 생성자를 호출해 부모 쪽 초기화를 대신 맡기는 표현이다
- 자식이 부모와 **같은 이름의 메서드를 다시 정의**하면 자식 것이 우선한다 — 이를 **오버라이딩(Overriding, 재정의)** 이라 한다
- Student가 hello를 재정의했다면 Student("철수", "한국중").hello() 는 부모 것이 아닌 자식의 hello가 실행된다
- print(isinstance(st, Person)) 의 결과는 True — 자식 객체는 부모 타입 검사도 통과한다
- 클래스 변수는 모든 객체가 **공유**한다. 생성자마다 Counter.cnt += 1 하면 객체를 2번 만든 뒤 Counter.cnt는 2 (객체 수 세기 단골)

## 얕은 복사 vs 깊은 복사 (실기 함정 최빈출)

리스트 변수는 값 자체가 아니라 **값이 있는 곳의 주소(참조)** 를 담는다. 이 성질 때문에 "복사했다고 생각했는데 원본이 바뀌는" 함정 문제가 반복 출제된다.

### 1단계: 복사가 아닌 별명 붙이기 (대입)

- b = a 는 복사가 아니라 **같은 리스트에 별명 하나를 더 붙인 것**이다
- a = [1, 2, 3] 에서 b = a 후 b.append(4) 하면 print(a) 의 결과도 [1, 2, 3, 4] — 원본이 함께 변한다

### 2단계: 얕은 복사(Shallow Copy)

- **copy() 메서드나 슬라이싱 a[:]** 는 겉상자를 새로 만드는 **얕은 복사**다
- c = a.copy() 후 c.append(5) 해도 a는 그대로다 — 1차원 리스트에서는 안전하다
- x = [1, 2] 에서 y = x[:] 후 y.append(3) 하면 print(x, y) 의 결과는 [1, 2] [1, 2, 3]

### 3단계: 얕은 복사의 함정 — 중첩 리스트

- 얕은 복사는 **겉상자만 새로 만들고, 안에 든 내부 리스트는 원본과 공유**한다. 서류 가방만 새로 사고 안의 서류 뭉치는 같은 것을 넣어 둔 셈이다
- m = [[1, 2], [3, 4]] 에서 sh = m.copy() 후 sh[0].append(99) 하면 print(m) 의 결과는 **[[1, 2, 99], [3, 4]]** — 원본까지 바뀐다
- 내부까지 전부 새로 만드는 것이 **깊은 복사(Deep Copy)** 이며, import copy 후 **copy.deepcopy(m)** 을 쓴다. 깊은 복사본을 수정해도 원본 m은 변하지 않는다

| 방식 | 표현 | 겉상자 | 내부 리스트 |
| --- | --- | --- | --- |
| 대입 | b = a | 공유 | 공유 |
| **얕은 복사** | a.copy(), a[:] | 새로 생성 | **공유 (함정)** |
| **깊은 복사** | copy.deepcopy(a) | 새로 생성 | 새로 생성 |

## 출제 포인트

| 포인트 | 핵심 정리 |
| --- | --- |
| __init__ | 객체 생성 시 자동 호출되는 **생성자** |
| self | 객체 자신, 메서드 첫 매개변수 (호출 시엔 생략) |
| 상속 표기 | class 자식(부모):, 부모 생성자는 super().__init__() |
| 오버라이딩 | 자식이 같은 이름 메서드 재정의 → 자식 것 실행 |
| b = a 대입 | 별명일 뿐, 한쪽 수정이 양쪽에 반영 |
| 얕은 복사 함정 | copy()는 내부 리스트 공유 → 중첩 수정 시 원본 변경, 해결은 deepcopy |
`,
    },
  ],
}
