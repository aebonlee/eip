export const industrialChapters = {
  'info-sys': [
    {
      id: 'ch1',
      name: '운영체제',
      topics: ['프로세스 관리', '스케줄링', '교착상태', '메모리 관리', '가상메모리', '페이지 교체'],
      content: `## 운영체제 (Operating System)

### 프로세스 관리
- **프로세스**: 실행 중인 프로그램으로, PCB(Process Control Block)에 의해 관리됨
- **프로세스 상태 전이**: 생성(New) → 준비(Ready) → 실행(Running) → 대기(Waiting) → 완료(Terminated)
- **디스패치(Dispatch)**: 준비 상태에서 실행 상태로 전환 (준비 → 실행)
- **타이머 런아웃(Timer Runout)**: 할당 시간 초과로 실행에서 준비로 전환 (실행 → 준비)
- **스레드(Thread)**: 프로세스 내의 실행 단위로, 경량 프로세스(Light Weight Process)라고도 함
- **PCB 구성 요소**: 프로세스 ID, 프로세스 상태, 프로그램 카운터, 레지스터, 메모리 정보 등

### CPU 스케줄링
- **비선점(Non-preemptive) 스케줄링**
  - **FCFS(First Come First Served)**: 도착 순서대로 처리, 호위 효과(Convoy Effect) 발생 가능
  - **SJF(Shortest Job First)**: 실행 시간이 가장 짧은 작업 우선, 기아(Starvation) 현상 발생 가능
  - **HRN(Highest Response-ratio Next)**: (대기시간 + 서비스시간) / 서비스시간, 기아 현상 해결
  - **우선순위(Priority)**: 우선순위가 높은 프로세스 먼저 실행
- **선점(Preemptive) 스케줄링**
  - **RR(Round Robin)**: 시간 할당량(Time Quantum)만큼 순환 실행, 시분할 시스템에 적합
  - **SRT(Shortest Remaining Time)**: 남은 실행 시간이 가장 짧은 프로세스 우선
  - **다단계 큐(Multi-Level Queue)**: 여러 준비 큐를 사용하여 그룹별 스케줄링
  - **다단계 피드백 큐(Multi-Level Feedback Queue)**: 큐 간 이동 가능, 가장 발전된 형태

### 교착상태 (Deadlock)
- **교착상태 발생 조건** (4가지 모두 만족 시 발생)
  - **상호 배제(Mutual Exclusion)**: 자원을 한 번에 하나의 프로세스만 사용
  - **점유와 대기(Hold and Wait)**: 자원을 보유한 채 다른 자원 요청
  - **비선점(Non-preemption)**: 다른 프로세스의 자원을 강제로 빼앗을 수 없음
  - **환형 대기(Circular Wait)**: 프로세스 간 자원 요청이 원형으로 구성
- **교착상태 해결 기법**: 예방(Prevention), 회피(Avoidance - 은행원 알고리즘), 탐지(Detection), 복구(Recovery)

### 메모리 관리
- **연속 할당**: 고정 분할(내부 단편화), 가변 분할(외부 단편화)
- **배치 전략**: **최초 적합(First Fit)**, **최적 적합(Best Fit)**, **최악 적합(Worst Fit)**
- **내부 단편화**: 할당된 공간 중 사용되지 않는 부분
- **외부 단편화**: 할당할 수 없는 작은 빈 공간들이 흩어져 있는 상태

### 가상메모리와 페이지 교체
- **가상메모리**: 보조기억장치를 주기억장치처럼 사용하는 기법
- **페이징(Paging)**: 고정 크기 블록(페이지)으로 분할, 내부 단편화 발생 가능
- **세그먼테이션(Segmentation)**: 가변 크기 블록(세그먼트)으로 분할, 외부 단편화 발생 가능
- **페이지 부재(Page Fault)**: 참조할 페이지가 주기억장치에 없는 상태
- **페이지 교체 알고리즘**
  - **FIFO**: 가장 먼저 들어온 페이지 교체, Belady의 모순 발생 가능
  - **LRU(Least Recently Used)**: 가장 오래 사용되지 않은 페이지 교체
  - **LFU(Least Frequently Used)**: 사용 빈도가 가장 적은 페이지 교체
  - **OPT(Optimal)**: 앞으로 가장 오래 사용되지 않을 페이지 교체 (이론적 최적)
- **스래싱(Thrashing)**: 페이지 부재가 과도하게 발생하여 CPU 이용률이 급격히 떨어지는 현상
- **워킹 셋(Working Set)**: 일정 시간 동안 자주 참조되는 페이지의 집합`
    },
    {
      id: 'ch2',
      name: '데이터 통신',
      topics: ['OSI 7계층', 'TCP/IP', '전송방식', '다중화', '오류제어'],
      content: `## 데이터 통신

### OSI 7계층 (Open Systems Interconnection)
- **물리 계층 (Physical Layer)**: 전기적/기계적 신호 전송, 비트(Bit) 단위
  - 장비: 리피터, 허브
  - 프로토콜: RS-232C, X.21
- **데이터 링크 계층 (Data Link Layer)**: 인접 노드 간 프레임(Frame) 전송, 오류/흐름 제어
  - 장비: 브리지, 스위치
  - 프로토콜: HDLC, PPP, 이더넷(Ethernet)
- **네트워크 계층 (Network Layer)**: 경로 설정(라우팅), 패킷(Packet) 단위
  - 장비: 라우터
  - 프로토콜: IP, ICMP, ARP, RARP, IGMP
- **전송 계층 (Transport Layer)**: 종단 간 신뢰성 있는 데이터 전송, 세그먼트(Segment)
  - 프로토콜: **TCP**(신뢰성, 연결 지향), **UDP**(비연결, 빠른 전송)
- **세션 계층 (Session Layer)**: 대화 제어, 동기점(체크포인트) 설정
- **표현 계층 (Presentation Layer)**: 데이터 형식 변환, 암호화/복호화, 압축
- **응용 계층 (Application Layer)**: 사용자 인터페이스 제공
  - 프로토콜: HTTP, FTP, SMTP, DNS, Telnet, SNMP

### TCP/IP 4계층
- **네트워크 액세스 계층**: OSI 물리 + 데이터링크 (이더넷, Wi-Fi)
- **인터넷 계층**: OSI 네트워크 (IP, ICMP, ARP)
- **전송 계층**: OSI 전송 (TCP, UDP)
- **응용 계층**: OSI 세션 + 표현 + 응용 (HTTP, FTP, DNS)

### TCP vs UDP
- **TCP**: 연결 지향, 3-way handshake(SYN→SYN+ACK→ACK), 신뢰성 보장, 흐름 제어, 혼잡 제어
- **UDP**: 비연결, 빠른 전송, 실시간 서비스(스트리밍, VoIP)에 적합

### 전송 방식
- **단방향(Simplex)**: 한 방향으로만 전송 (TV, 라디오)
- **반이중(Half-Duplex)**: 양방향이나 동시 불가 (무전기)
- **전이중(Full-Duplex)**: 양방향 동시 전송 (전화)
- **직렬 전송**: 한 비트씩 순차 전송
- **병렬 전송**: 여러 비트 동시 전송

### 다중화 (Multiplexing)
- **FDM(주파수 분할 다중화)**: 주파수 대역을 분할하여 할당
- **TDM(시분할 다중화)**: 시간을 분할하여 할당 (동기식/비동기식)
- **CDM(코드 분할 다중화)**: 고유 코드를 부여하여 구분 (CDMA)
- **WDM(파장 분할 다중화)**: 광섬유에서 파장을 분할

### 오류 제어
- **오류 검출**: 패리티 비트, CRC(순환 중복 검사), 체크섬(Checksum)
- **오류 정정**: 해밍 코드(Hamming Code) - 1비트 오류 정정 가능
- **ARQ(자동 반복 요청)**: Stop-and-Wait, Go-Back-N, Selective Repeat
- **흐름 제어**: 슬라이딩 윈도우(Sliding Window), Stop-and-Wait`
    },
    {
      id: 'ch3',
      name: '컴퓨터 구조',
      topics: ['CPU 구조', '레지스터', '명령어 사이클', '주소지정 방식', '캐시 메모리', '인터럽트', '버스'],
      content: `## 컴퓨터 구조

### CPU 구조
- **CPU 구성 요소**: 산술논리연산장치(ALU), 제어장치(CU), 레지스터(Register)
- **ALU**: 산술 연산(+, -, ×, ÷)과 논리 연산(AND, OR, NOT, XOR) 수행
- **제어장치(CU)**: 명령어를 해독하고 제어 신호를 발생시켜 각 장치를 제어
- **마이크로프로세서**: CPU를 하나의 IC 칩에 집적한 것

### 레지스터 (Register)
- **프로그램 카운터(PC)**: 다음 실행할 명령어의 주소를 저장
- **명령어 레지스터(IR)**: 현재 실행 중인 명령어를 저장
- **메모리 주소 레지스터(MAR)**: 접근할 메모리의 주소를 저장
- **메모리 버퍼 레지스터(MBR)**: 메모리에서 읽거나 쓸 데이터를 저장
- **누산기(Accumulator)**: 연산 결과를 일시적으로 저장
- **상태 레지스터(Status Register)**: 연산 결과의 상태(캐리, 오버플로, 제로 등) 저장
- **베이스 레지스터**: 기준 주소 저장
- **인덱스 레지스터**: 주소 변경을 위한 값 저장

### 명령어 사이클 (Instruction Cycle)
- **인출(Fetch)**: 메모리에서 명령어를 가져옴
- **해독(Decode)**: 명령어를 해독
- **실행(Execute)**: 명령어 수행
- **저장(Store)**: 결과를 저장
- **명령어 형식**: 연산코드(Op-code) + 오퍼랜드(Operand)

### 주소지정 방식 (Addressing Mode)
- **즉시 주소지정(Immediate)**: 오퍼랜드가 실제 데이터, 빠르지만 데이터 크기 제한
- **직접 주소지정(Direct)**: 오퍼랜드가 데이터의 메모리 주소
- **간접 주소지정(Indirect)**: 오퍼랜드가 데이터 주소의 주소, 메모리 접근 2회
- **레지스터 주소지정(Register)**: 오퍼랜드가 레지스터 번호
- **레지스터 간접 주소지정(Register Indirect)**: 레지스터가 데이터 주소를 저장
- **인덱스 주소지정(Indexed)**: 인덱스 레지스터 값 + 변위 = 유효 주소
- **베이스 레지스터 주소지정(Base Register)**: 베이스 레지스터 값 + 변위 = 유효 주소

### 캐시 메모리 (Cache Memory)
- **캐시**: CPU와 주기억장치 사이의 고속 버퍼 메모리, 속도 차이 해결
- **적중률(Hit Ratio)**: 캐시에서 데이터를 찾을 확률
- **매핑 방식**: 직접 매핑(Direct), 연관 매핑(Associative), 집합 연관 매핑(Set-Associative)
- **기억장치 계층 구조**: 레지스터 → 캐시 → 주기억장치(RAM/ROM) → 보조기억장치(HDD/SSD)
  - 상위: 속도 빠름, 용량 작음, 비용 높음
  - 하위: 속도 느림, 용량 큼, 비용 낮음
- **지역성(Locality)**: 시간적 지역성(최근 사용 데이터 재사용), 공간적 지역성(인접 데이터 사용)

### 인터럽트 (Interrupt)
- **인터럽트**: 프로그램 실행 중 예기치 않은 상황 발생 시 현재 작업을 중단하고 처리
- **외부 인터럽트**: 전원 이상, I/O, 타이머 등 하드웨어적 요인
- **내부 인터럽트(트랩)**: 오버플로, 0으로 나누기 등 프로그램 오류
- **소프트웨어 인터럽트(SVC)**: 시스템 호출(System Call)에 의한 인터럽트

### 버스 (Bus)
- **데이터 버스**: CPU와 메모리/입출력장치 간 데이터 전송 (양방향)
- **주소 버스**: CPU가 메모리/입출력장치의 주소를 전달 (단방향)
- **제어 버스**: 제어 신호 전달 (양방향)`
    },
    {
      id: 'ch4',
      name: '소프트웨어 공학',
      topics: ['SDLC 모델', '요구분석', '설계', '테스트', '유지보수', '품질보증', '프로젝트 관리'],
      content: `## 소프트웨어 공학

### SDLC (소프트웨어 개발 생명주기 모델)
- **폭포수 모델(Waterfall)**: 순차적 진행, 각 단계 완료 후 다음 단계, 되돌리기 어려움, 가장 오래된 전통적 모델
- **프로토타입 모델(Prototype)**: 시제품(원형)을 만들어 요구사항 확인, 사용자 피드백 반영
- **나선형 모델(Spiral)**: **위험 분석** 포함, 계획→위험분석→개발→평가 반복, 대규모 프로젝트에 적합
- **V 모델**: 폭포수 모델의 변형, 각 개발 단계에 대응하는 **테스트 단계**를 명시
- **애자일(Agile)**: 반복적/점진적 개발, 고객 협력, 변화 대응 중시
  - **스크럼(Scrum)**: 스프린트(Sprint) 단위 반복, 데일리 스크럼 회의
  - **XP(eXtreme Programming)**: 짝 프로그래밍, TDD, 리팩토링, 지속적 통합(CI)
- **RAD(Rapid Application Development)**: 짧은 개발 주기, CASE 도구 활용

### 요구분석
- **기능적 요구사항**: 시스템이 수행해야 할 기능
- **비기능적 요구사항**: 성능, 보안, 가용성, 확장성 등 품질 관련
- **요구분석 도구**: DFD(자료흐름도), DD(자료사전), ERD(개체관계도), STD(상태전이도)
- **DFD 구성 요소**: 처리(Process), 자료흐름(Data Flow), 자료저장소(Data Store), 외부 엔티티(External Entity)

### 설계
- **상위 설계(아키텍처 설계)**: 시스템 구조, 모듈 분할, 인터페이스 정의
- **하위 설계(상세 설계)**: 모듈 내부 알고리즘, 자료구조 설계
- **모듈화**: **결합도(Coupling)**는 낮을수록, **응집도(Cohesion)**는 높을수록 좋음
- **결합도** (낮음→높음): 자료 → 스탬프 → 제어 → 외부 → 공통 → 내용
- **응집도** (높음→낮음): 기능적 → 순차적 → 교환적 → 절차적 → 시간적 → 논리적 → 우연적
- **팬인(Fan-In)**: 모듈을 호출하는 모듈의 수 (높을수록 좋음)
- **팬아웃(Fan-Out)**: 모듈이 호출하는 모듈의 수 (낮을수록 좋음)

### 테스트
- **화이트박스 테스트**: 내부 구조/로직 기반, 기초 경로 검사, 조건/분기 검사
- **블랙박스 테스트**: 외부 기능 기반, 입출력 중심
  - **동치 분할(Equivalence Partitioning)**: 입력 데이터를 유효/무효 클래스로 분류
  - **경계값 분석(Boundary Value Analysis)**: 경계 부분의 값을 테스트
  - **원인-결과 그래프**: 입력 조건과 결과 간의 관계 분석
- **테스트 단계**: 단위 테스트 → 통합 테스트 → 시스템 테스트 → 인수 테스트
- **통합 테스트**: 상향식(Bottom-up, 드라이버 사용), 하향식(Top-down, 스텁 사용)

### 유지보수 및 품질보증
- **유지보수 유형**: 수정(Corrective), 적응(Adaptive), 완전(Perfective), 예방(Preventive)
- **소프트웨어 품질 특성(ISO/IEC 9126)**: 기능성, 신뢰성, 사용성, 효율성, 유지보수성, 이식성
- **CMM(Capability Maturity Model)**: 초기 → 반복 → 정의 → 관리 → 최적화 (5단계)

### 프로젝트 관리
- **비용 산정 기법**: LOC(코드 라인 수), COCOMO(유기/반분리/내장형), 기능점수(FP)
- **일정 관리**: PERT(비관/낙관/기대치), CPM(임계경로), 간트 차트(Gantt Chart)
- **위험 관리**: 위험 식별 → 분석 → 계획 → 감시`
    }
  ],
  'programming': [
    {
      id: 'ch1',
      name: 'C언어',
      topics: ['변수/상수', '자료형', '연산자', '제어문', '함수', '배열', '포인터', '구조체', '파일처리'],
      content: `## C언어

### 변수와 상수
- **변수**: 데이터를 저장하는 메모리 공간, 선언 후 사용
- **상수**: 값이 변하지 않는 데이터, \`const\` 키워드 또는 \`#define\` 매크로
- **변수명 규칙**: 영문/숫자/밑줄(_), 첫 글자 숫자 불가, 예약어 사용 불가

### 자료형
- **정수형**: \`char\`(1바이트), \`short\`(2바이트), \`int\`(4바이트), \`long\`(4바이트)
- **실수형**: \`float\`(4바이트), \`double\`(8바이트)
- **형변환**: 암시적(자동), 명시적(캐스팅) - \`(int)3.14\` → 3

### 연산자
- **산술 연산자**: +, -, *, /, %(나머지)
- **관계 연산자**: ==, !=, <, >, <=, >=
- **논리 연산자**: &&(AND), ||(OR), !(NOT)
- **비트 연산자**: &(AND), |(OR), ^(XOR), ~(NOT), <<(좌시프트), >>(우시프트)
- **증감 연산자**: ++a(전위), a++(후위) — 전위는 먼저 증가 후 사용, 후위는 사용 후 증가
- **삼항 연산자**: \`조건 ? 참 : 거짓\`
- **연산자 우선순위**: 단항 → 산술 → 시프트 → 관계 → 비트 → 논리 → 삼항 → 대입

### 제어문
- **조건문**: \`if\`, \`if-else\`, \`if-else if-else\`, \`switch-case\` (break 필수)
- **반복문**: \`for\`, \`while\`, \`do-while\` (최소 1회 실행 보장)
- **분기문**: \`break\`(반복 종료), \`continue\`(다음 반복으로), \`return\`(함수 종료), \`goto\`

### 함수
- **함수 정의**: \`반환형 함수명(매개변수) { 본문; return 값; }\`
- **호출 방식**: **Call by Value**(값 전달, 원본 불변), **Call by Reference**(주소 전달, 원본 변경)
- **재귀 함수(Recursive)**: 자기 자신을 호출하는 함수, 반드시 종료 조건 필요
- **기억 클래스**: auto(자동), static(정적 - 함수 종료 후에도 유지), extern(외부), register

### 배열
- **1차원 배열**: \`int arr[5] = {1, 2, 3, 4, 5};\` 인덱스는 0부터 시작
- **2차원 배열**: \`int arr[2][3];\` 행×열 구조
- **문자열 배열**: \`char str[] = "Hello";\` — 끝에 널문자(\\0) 자동 포함
- **문자열 함수**: \`strlen\`(길이), \`strcpy\`(복사), \`strcmp\`(비교), \`strcat\`(연결)

### 포인터
- **포인터**: 메모리 주소를 저장하는 변수, \`int *p;\`
- **주소 연산자(&)**: 변수의 주소를 반환
- **간접 참조 연산자(*)**: 포인터가 가리키는 값에 접근
- **배열과 포인터**: \`arr[i]\` = \`*(arr + i)\`, 배열명은 첫 번째 요소의 주소
- **포인터 배열**: \`int *arr[3];\` — 포인터를 원소로 갖는 배열
- **이중 포인터**: \`int **pp;\` — 포인터의 주소를 저장

### 구조체
- **구조체(struct)**: 서로 다른 자료형을 하나로 묶은 사용자 정의 자료형
- **선언**: \`struct Student { char name[20]; int age; };\`
- **접근**: 직접 접근(\`.\`), 포인터 접근(\`->\`)
- **공용체(union)**: 멤버들이 같은 메모리 공간 공유, 가장 큰 멤버 크기만큼 할당

### 파일 처리
- **파일 열기/닫기**: \`fopen(파일명, 모드)\`, \`fclose(fp)\`
- **모드**: "r"(읽기), "w"(쓰기), "a"(추가), "r+"(읽기/쓰기)
- **입출력 함수**: \`fprintf\`, \`fscanf\`, \`fgets\`, \`fputs\`, \`fread\`, \`fwrite\``
    },
    {
      id: 'ch2',
      name: 'Java',
      topics: ['클래스/객체', '상속', '다형성', '인터페이스', '예외처리', '컬렉션', '입출력', '스레드'],
      content: `## Java

### 클래스와 객체
- **클래스(Class)**: 객체를 생성하기 위한 설계도(틀), 속성(필드)과 행위(메서드)로 구성
- **객체(Object)**: 클래스의 인스턴스, \`new\` 키워드로 생성
- **생성자(Constructor)**: 객체 생성 시 자동 호출되는 메서드, 클래스명과 동일, 반환형 없음
- **this**: 현재 객체 자신을 참조하는 키워드
- **접근 제어자**: \`public\`(모든 접근), \`protected\`(같은 패키지+자식 클래스), \`default\`(같은 패키지), \`private\`(같은 클래스)
- **static**: 클래스 변수/메서드, 객체 생성 없이 클래스명으로 직접 접근 가능

### 상속 (Inheritance)
- **상속**: 기존 클래스(부모)의 속성과 메서드를 새 클래스(자식)가 물려받음
- **extends** 키워드 사용: \`class Child extends Parent { }\`
- **super**: 부모 클래스의 생성자나 메서드를 호출할 때 사용
- **오버라이딩(Overriding)**: 부모 클래스의 메서드를 자식 클래스에서 재정의
- **오버로딩(Overloading)**: 같은 이름의 메서드를 매개변수의 타입/개수를 다르게 정의
- **final**: 클래스(상속 불가), 메서드(오버라이딩 불가), 변수(값 변경 불가)
- Java는 **단일 상속**만 지원 (다중 상속은 인터페이스로 구현)

### 다형성 (Polymorphism)
- **다형성**: 하나의 객체가 여러 형태를 가질 수 있는 특성
- **업캐스팅**: 자식 객체를 부모 타입으로 형변환 (자동)
- **다운캐스팅**: 부모 타입을 자식 타입으로 형변환 (명시적)
- **instanceof**: 객체의 타입을 확인하는 연산자

### 추상 클래스와 인터페이스
- **추상 클래스(abstract class)**: 추상 메서드를 포함, 직접 인스턴스 생성 불가
- **추상 메서드**: 선언만 있고 본문이 없는 메서드, 자식 클래스에서 반드시 구현
- **인터페이스(interface)**: 모든 메서드가 추상 메서드, 다중 구현(implements) 가능
- **인터페이스 특징**: 상수(public static final)와 추상 메서드(public abstract)만 포함

### 예외 처리 (Exception Handling)
- **예외**: 프로그램 실행 중 발생하는 비정상적인 상황
- **try-catch-finally**: try(예외 발생 가능 코드), catch(예외 처리), finally(항상 실행)
- **throw**: 의도적으로 예외를 발생시킴
- **throws**: 메서드 선언부에서 예외를 호출자에게 전달
- **예외 클래스 계층**: Throwable → Error / Exception → RuntimeException / Checked Exception

### 컬렉션 프레임워크
- **List**: 순서 있고 중복 허용 (ArrayList, LinkedList, Vector)
- **Set**: 순서 없고 중복 불허 (HashSet, TreeSet)
- **Map**: 키-값 쌍, 키 중복 불허 (HashMap, TreeMap, Hashtable)
- **Iterator**: 컬렉션 요소를 순차적으로 접근하는 인터페이스

### 입출력 (I/O)
- **바이트 스트림**: InputStream / OutputStream (바이너리 데이터)
- **문자 스트림**: Reader / Writer (텍스트 데이터)
- **보조 스트림**: BufferedReader, BufferedWriter (성능 향상)
- **Scanner**: 입력을 쉽게 처리하는 클래스

### 스레드 (Thread)
- **스레드**: 프로세스 내의 실행 흐름
- **생성 방법**: Thread 클래스 상속, Runnable 인터페이스 구현
- **동기화(synchronized)**: 공유 자원에 대한 동시 접근 제어
- **스레드 상태**: NEW → RUNNABLE → RUNNING → WAITING/BLOCKED → TERMINATED`
    },
    {
      id: 'ch3',
      name: 'Python',
      topics: ['기본문법', '리스트/튜플/딕셔너리/집합', '함수', '클래스', '모듈', '파일처리', '라이브러리'],
      content: `## Python

### 기본 문법
- **인터프리터 언어**: 컴파일 없이 한 줄씩 해석하여 실행
- **들여쓰기(Indentation)**: 코드 블록을 중괄호 대신 들여쓰기로 구분 (필수)
- **변수 선언**: 별도 자료형 선언 없이 값 할당으로 생성 (동적 타이핑)
- **자료형**: int(정수), float(실수), str(문자열), bool(논리), None(널)
- **출력**: \`print()\`, **입력**: \`input()\` (항상 문자열로 반환)
- **문자열**: 작은따옴표(''), 큰따옴표(""), 삼중따옴표(''' ''', """ """)
- **형변환**: \`int()\`, \`float()\`, \`str()\`, \`bool()\`
- **슬라이싱**: \`문자열[시작:끝:증가값]\` — 끝 인덱스는 포함하지 않음

### 리스트, 튜플, 딕셔너리, 집합
- **리스트(List)**: 순서 있는 가변(mutable) 시퀀스, \`[1, 2, 3]\`
  - 메서드: \`append()\`, \`insert()\`, \`remove()\`, \`pop()\`, \`sort()\`, \`reverse()\`, \`extend()\`
  - **리스트 컴프리헨션**: \`[x*2 for x in range(5)]\` → [0, 2, 4, 6, 8]
- **튜플(Tuple)**: 순서 있는 불변(immutable) 시퀀스, \`(1, 2, 3)\`
  - 변경 불가, 리스트보다 처리 속도 빠름, 딕셔너리 키로 사용 가능
- **딕셔너리(Dictionary)**: 키-값 쌍, \`{"name": "홍길동", "age": 25}\`
  - 메서드: \`keys()\`, \`values()\`, \`items()\`, \`get()\`, \`update()\`
- **집합(Set)**: 순서 없고 중복 불허, \`{1, 2, 3}\`
  - 연산: 합집합(\`|\`), 교집합(\`&\`), 차집합(\`-\`), 대칭차집합(\`^\`)

### 함수
- **정의**: \`def 함수명(매개변수):\` — return문으로 값 반환
- **기본값 매개변수**: \`def func(a, b=10):\` — b의 기본값은 10
- **가변 인자**: \`*args\`(튜플), \`**kwargs\`(딕셔너리)
- **람다(Lambda)**: 이름 없는 한 줄 함수, \`lambda x, y: x + y\`
- **전역/지역 변수**: 함수 내부에서 전역변수 수정 시 \`global\` 키워드 사용
- **내장 함수**: \`len()\`, \`range()\`, \`map()\`, \`filter()\`, \`zip()\`, \`enumerate()\`

### 클래스
- **클래스 정의**: \`class 클래스명:\`
- **생성자**: \`__init__(self)\` 메서드
- **self**: 인스턴스 자기 자신을 참조 (Java의 this와 유사)
- **상속**: \`class Child(Parent):\` — 다중 상속 지원
- **메서드 오버라이딩**: 부모 클래스의 메서드를 자식 클래스에서 재정의
- **super()**: 부모 클래스의 메서드 호출

### 모듈과 패키지
- **모듈**: 함수, 변수, 클래스를 모아놓은 .py 파일
- **import**: \`import 모듈명\`, \`from 모듈 import 함수\`, \`import 모듈 as 별칭\`
- **패키지**: 모듈을 디렉토리 구조로 묶은 것 (__init__.py 파일 포함)
- **주요 표준 모듈**: os, sys, math, random, datetime, json

### 파일 처리
- **열기/닫기**: \`open(파일명, 모드)\`, \`close()\`
- **with문**: \`with open('file.txt', 'r') as f:\` — 자동으로 파일 닫기
- **모드**: 'r'(읽기), 'w'(쓰기), 'a'(추가), 'rb'/'wb'(바이너리)
- **메서드**: \`read()\`, \`readline()\`, \`readlines()\`, \`write()\`, \`writelines()\`

### 주요 라이브러리
- **NumPy**: 다차원 배열과 수학 연산
- **Pandas**: 데이터 분석 (DataFrame, Series)
- **Matplotlib**: 데이터 시각화 (그래프, 차트)`
    },
    {
      id: 'ch4',
      name: '자료구조',
      topics: ['배열', '스택', '큐', '연결리스트', '트리', '그래프', '정렬', '탐색'],
      content: `## 자료구조

### 배열 (Array)
- **배열**: 같은 자료형의 데이터를 연속된 메모리 공간에 저장
- **특징**: 인덱스로 직접 접근(O(1)), 삽입/삭제 비효율적(O(n)), 크기 고정
- **희소 행렬(Sparse Matrix)**: 대부분 0인 행렬, 3원소 쌍(행, 열, 값)으로 효율적 저장

### 스택 (Stack)
- **스택**: **LIFO(Last In First Out)** 구조, 후입선출
- **연산**: \`push\`(삽입), \`pop\`(삭제), \`peek/top\`(조회)
- **활용**: 함수 호출 관리(시스템 스택), 후위 표기법 변환/계산, 괄호 검사, DFS, 되돌리기(Undo)
- **오버플로(Overflow)**: 스택이 가득 찬 상태에서 push 시도
- **언더플로(Underflow)**: 스택이 비어있는 상태에서 pop 시도

### 큐 (Queue)
- **큐**: **FIFO(First In First Out)** 구조, 선입선출
- **연산**: \`enqueue\`(삽입), \`dequeue\`(삭제)
- **활용**: 프로세스 스케줄링, 버퍼(Buffer), BFS, 대기열 관리
- **원형 큐(Circular Queue)**: 배열의 처음과 끝이 연결된 형태, 메모리 효율적 사용
- **우선순위 큐(Priority Queue)**: 우선순위에 따라 처리, 힙(Heap)으로 구현
- **덱(Deque)**: 양쪽 끝에서 삽입/삭제 가능

### 연결 리스트 (Linked List)
- **연결 리스트**: 각 노드가 데이터와 다음 노드의 포인터(링크)로 구성
- **단순 연결 리스트**: 한 방향으로만 연결
- **이중 연결 리스트**: 양방향 연결 (이전 노드 포인터 포함)
- **원형 연결 리스트**: 마지막 노드가 첫 번째 노드를 가리킴
- **특징**: 삽입/삭제 용이(O(1)), 탐색 비효율적(O(n)), 동적 크기 조절

### 트리 (Tree)
- **트리**: 계층적 구조, 노드(Node)와 간선(Edge)으로 구성
- **용어**: 루트(Root), 부모(Parent), 자식(Child), 형제(Sibling), 리프(Leaf), 차수(Degree), 깊이(Depth)
- **이진 트리(Binary Tree)**: 각 노드의 자식이 최대 2개
- **이진 트리 순회**
  - **전위(Preorder)**: 루트 → 왼쪽 → 오른쪽 (VLR)
  - **중위(Inorder)**: 왼쪽 → 루트 → 오른쪽 (LVR)
  - **후위(Postorder)**: 왼쪽 → 오른쪽 → 루트 (LRV)
- **이진 탐색 트리(BST)**: 왼쪽 < 루트 < 오른쪽, 탐색 평균 O(log n)
- **힙(Heap)**: 최대 힙(부모 ≥ 자식), 최소 힙(부모 ≤ 자식), 우선순위 큐 구현

### 그래프 (Graph)
- **그래프**: 정점(Vertex)과 간선(Edge)의 집합, G = (V, E)
- **방향 그래프(Directed)** vs **무방향 그래프(Undirected)**
- **인접 행렬**: 2차원 배열로 표현, 공간 O(V²)
- **인접 리스트**: 연결 리스트로 표현, 공간 O(V+E)
- **탐색**: **DFS**(깊이 우선, 스택 사용), **BFS**(너비 우선, 큐 사용)
- **최소 신장 트리(MST)**: 크루스칼(Kruskal), 프림(Prim) 알고리즘
- **최단 경로**: 다익스트라(Dijkstra), 벨만-포드(Bellman-Ford)

### 정렬 (Sorting)
- **버블 정렬(Bubble Sort)**: 인접 요소 비교 교환, O(n²), 안정 정렬
- **선택 정렬(Selection Sort)**: 최솟값을 찾아 앞에서부터 교환, O(n²), 불안정 정렬
- **삽입 정렬(Insertion Sort)**: 적절한 위치에 삽입, O(n²), 안정 정렬, 거의 정렬된 데이터에 효율적
- **퀵 정렬(Quick Sort)**: 피벗(Pivot) 기준 분할, 평균 O(n log n), 최악 O(n²)
- **병합 정렬(Merge Sort)**: 분할 정복, 항상 O(n log n), 안정 정렬, 추가 메모리 필요

### 탐색 (Searching)
- **순차 탐색(Sequential Search)**: 처음부터 순서대로, O(n)
- **이진 탐색(Binary Search)**: 정렬된 데이터에서 반씩 분할, O(log n)
- **해시 탐색(Hash Search)**: 해시 함수로 직접 접근, 평균 O(1)
- **해시 충돌 해결**: 체이닝(Chaining), 개방 주소법(Open Addressing - 선형/이차/이중 해싱)`
    }
  ],
  'db': [
    {
      id: 'ch1',
      name: '데이터 모델링',
      topics: ['개체-관계 모델', '관계 데이터 모델', '속성/엔티티/관계', '매핑 규칙', '논리적 설계'],
      content: `## 데이터 모델링

### 데이터 모델 개요
- **데이터 모델**: 현실 세계의 데이터를 컴퓨터에 표현하기 위한 추상화 도구
- **데이터 모델 구성 요소**: 개체(Entity), 속성(Attribute), 관계(Relationship)
- **데이터 모델링 단계**: 개념적 설계 → 논리적 설계 → 물리적 설계

### 개체-관계 모델 (E-R Model)
- **개체(Entity)**: 현실 세계에서 독립적으로 존재하는 유무형의 정보, 사각형으로 표현
- **속성(Attribute)**: 개체의 특성, 타원으로 표현
  - **기본 속성**: 본래 가지고 있는 속성
  - **설계 속성**: 설계 과정에서 새로 만든 속성
  - **파생 속성(Derived)**: 다른 속성으로부터 계산되는 속성 (점선 타원)
  - **다중값 속성(Multi-valued)**: 여러 값을 가질 수 있는 속성 (이중 타원)
  - **복합 속성(Composite)**: 더 작은 속성으로 분해 가능
- **관계(Relationship)**: 개체 간의 연관성, 마름모로 표현
- **카디널리티(Cardinality)**: 1:1, 1:N, N:M 관계

### 관계 데이터 모델
- **릴레이션(Relation)**: 2차원 테이블 형태
- **구성 요소**
  - **튜플(Tuple)**: 행(Row), 레코드, 카디널리티(튜플의 수)
  - **속성(Attribute)**: 열(Column), 필드, 차수(Degree, 속성의 수)
  - **도메인(Domain)**: 속성이 가질 수 있는 값의 범위
- **키(Key)의 종류**
  - **슈퍼키(Super Key)**: 유일성을 만족하는 속성 집합
  - **후보키(Candidate Key)**: 유일성 + 최소성을 만족
  - **기본키(Primary Key)**: 후보키 중 선택된 키, NULL 불가, 중복 불가
  - **대체키(Alternate Key)**: 후보키 중 기본키를 제외한 키
  - **외래키(Foreign Key)**: 다른 릴레이션의 기본키를 참조하는 속성

### 관계 대수와 관계 해석
- **관계 대수(Relational Algebra)**: 절차적 언어, 연산 순서 명시
  - **일반 집합 연산**: 합집합(∪), 교집합(∩), 차집합(-), 카티션 곱(×)
  - **순수 관계 연산**: 셀렉트(σ, 행 선택), 프로젝트(π, 열 선택), 조인(⋈), 디비전(÷)
- **관계 해석(Relational Calculus)**: 비절차적 언어, 원하는 결과만 명시

### 매핑 규칙 (E-R → 관계 모델 변환)
- **1:1 관계**: 어느 쪽이든 외래키를 추가하거나 릴레이션 통합
- **1:N 관계**: N측 릴레이션에 1측의 기본키를 외래키로 추가
- **N:M 관계**: 새로운 릴레이션(교차 테이블) 생성, 양쪽 기본키를 외래키로 포함
- **다중값 속성**: 별도의 릴레이션으로 분리

### 논리적 설계
- 개념적 스키마(E-R 다이어그램)를 논리적 데이터 모델(관계형, 계층형, 네트워크형)로 변환
- **관계형 모델**: 테이블 형태, 가장 널리 사용
- **계층형 모델**: 트리 구조, 1:N 관계 표현
- **네트워크형 모델**: 그래프 구조, N:M 관계 표현`
    },
    {
      id: 'ch2',
      name: 'SQL',
      topics: ['DDL', 'DML', 'DCL', 'JOIN', '서브쿼리', '집계함수', 'GROUP BY/HAVING'],
      content: `## SQL (Structured Query Language)

### DDL (데이터 정의어)
- **CREATE**: 테이블/뷰/인덱스 생성
  - \`CREATE TABLE 테이블명 (컬럼명 자료형 [제약조건], ...);\`
  - **제약조건**: PRIMARY KEY, FOREIGN KEY, UNIQUE, NOT NULL, CHECK, DEFAULT
  - \`FOREIGN KEY (컬럼) REFERENCES 참조테이블(컬럼)\`
- **ALTER**: 테이블 구조 변경
  - \`ALTER TABLE 테이블명 ADD 컬럼명 자료형;\` (컬럼 추가)
  - \`ALTER TABLE 테이블명 MODIFY 컬럼명 자료형;\` (컬럼 수정)
  - \`ALTER TABLE 테이블명 DROP COLUMN 컬럼명;\` (컬럼 삭제)
- **DROP**: 테이블/뷰/인덱스 삭제
  - \`DROP TABLE 테이블명 [CASCADE | RESTRICT];\`
  - **CASCADE**: 참조하는 모든 객체도 함께 삭제
  - **RESTRICT**: 참조되는 경우 삭제 거부
- **TRUNCATE**: 테이블의 모든 행 삭제 (구조 유지, 복구 불가)

### DML (데이터 조작어)
- **SELECT**: 데이터 조회
  - \`SELECT [DISTINCT] 컬럼 FROM 테이블 [WHERE 조건] [ORDER BY 컬럼 ASC|DESC];\`
  - **DISTINCT**: 중복 행 제거
  - **WHERE 조건 연산자**: =, <>, <, >, BETWEEN, IN, LIKE, IS NULL
  - **LIKE 패턴**: %(0개 이상 문자), _(1개 문자) — \`LIKE '김%'\`: 김으로 시작
- **INSERT**: 데이터 삽입
  - \`INSERT INTO 테이블명(컬럼1, 컬럼2) VALUES(값1, 값2);\`
- **UPDATE**: 데이터 수정
  - \`UPDATE 테이블명 SET 컬럼1=값1 WHERE 조건;\`
- **DELETE**: 데이터 삭제
  - \`DELETE FROM 테이블명 WHERE 조건;\`

### DCL (데이터 제어어)
- **GRANT**: 권한 부여
  - \`GRANT 권한 ON 객체 TO 사용자 [WITH GRANT OPTION];\`
  - **WITH GRANT OPTION**: 부여받은 권한을 다른 사용자에게 재부여 가능
- **REVOKE**: 권한 회수
  - \`REVOKE 권한 ON 객체 FROM 사용자 [CASCADE];\`
- **TCL(트랜잭션 제어어)**: COMMIT(확정), ROLLBACK(취소), SAVEPOINT(저장점)

### JOIN
- **INNER JOIN**: 양쪽 테이블에서 조건에 맞는 행만 반환
  - \`SELECT * FROM A INNER JOIN B ON A.key = B.key;\`
- **LEFT OUTER JOIN**: 왼쪽 테이블의 모든 행 + 오른쪽 매칭 행
- **RIGHT OUTER JOIN**: 오른쪽 테이블의 모든 행 + 왼쪽 매칭 행
- **FULL OUTER JOIN**: 양쪽 테이블의 모든 행
- **CROSS JOIN**: 카티션 곱, 모든 조합
- **SELF JOIN**: 같은 테이블을 자기 자신과 조인
- **NATURAL JOIN**: 같은 이름의 컬럼을 자동으로 조인

### 서브쿼리 (Subquery)
- **서브쿼리**: SQL문 안에 포함된 또 다른 SQL문
- **단일행 서브쿼리**: 결과가 1행, 비교 연산자(=, <, >) 사용
- **다중행 서브쿼리**: 결과가 여러 행, IN, ANY, ALL, EXISTS 사용
  - **IN**: 서브쿼리 결과 중 하나라도 일치
  - **ANY/SOME**: 서브쿼리 결과 중 하나라도 조건 만족
  - **ALL**: 서브쿼리 결과 모두 조건 만족
  - **EXISTS**: 서브쿼리 결과가 존재하면 참

### 집계함수와 그룹화
- **집계함수**: COUNT(행 수), SUM(합계), AVG(평균), MAX(최댓값), MIN(최솟값)
- **GROUP BY**: 특정 컬럼 기준으로 그룹화
- **HAVING**: GROUP BY로 그룹화한 결과에 조건 지정 (WHERE은 그룹화 전 조건)
- **실행 순서**: FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY`
    },
    {
      id: 'ch3',
      name: '정규화',
      topics: ['함수 종속', '1NF', '2NF', '3NF', 'BCNF', '이상현상', '반정규화'],
      content: `## 정규화 (Normalization)

### 이상현상 (Anomaly)
- **이상현상**: 잘못된 테이블 설계로 인해 데이터 조작 시 발생하는 문제
- **삽입 이상(Insertion Anomaly)**: 불필요한 데이터를 함께 삽입해야 하는 문제
- **삭제 이상(Deletion Anomaly)**: 의도하지 않은 데이터까지 함께 삭제되는 문제
- **갱신 이상(Update Anomaly)**: 일부만 수정하여 데이터 불일치가 발생하는 문제

### 함수 종속 (Functional Dependency)
- **함수 종속**: 속성 X의 값이 속성 Y의 값을 유일하게 결정하면 Y는 X에 함수 종속 (X → Y)
- **완전 함수 종속**: 기본키 전체에 종속 (기본키의 일부에는 종속되지 않음)
- **부분 함수 종속**: 기본키의 일부 속성에만 종속
- **이행적 함수 종속**: X → Y이고 Y → Z이면 X → Z (이행적 종속)
- **결정자(Determinant)**: 함수 종속에서 다른 속성의 값을 결정하는 속성 (X → Y에서 X)

### 정규화 단계
- **제1정규형(1NF)**: 모든 속성의 값이 **원자값(Atomic Value)**을 가져야 함
  - 반복 그룹이나 다중값 속성 제거
- **제2정규형(2NF)**: 1NF를 만족하고, **부분 함수 종속 제거**
  - 기본키가 복합키일 때 기본키의 일부에만 종속되는 속성을 분리
- **제3정규형(3NF)**: 2NF를 만족하고, **이행적 함수 종속 제거**
  - X → Y → Z에서 X → Z와 같은 이행적 종속 제거
- **BCNF(Boyce-Codd Normal Form)**: 3NF를 만족하고, **모든 결정자가 후보키**
  - 후보키가 아닌 속성이 결정자인 경우를 분리

### 정규화 요약
| 정규형 | 조건 |
|--------|------|
| 1NF | 원자값 |
| 2NF | 부분 함수 종속 제거 |
| 3NF | 이행적 함수 종속 제거 |
| BCNF | 결정자가 모두 후보키 |
| 4NF | 다치 종속 제거 |
| 5NF | 조인 종속 제거 |

### 정규화의 장단점
- **장점**: 데이터 중복 최소화, 이상현상 방지, 데이터 무결성 향상, 저장 공간 효율적 사용
- **단점**: 릴레이션 분해로 인한 JOIN 연산 증가, 조회 성능 저하 가능

### 반정규화 (De-normalization)
- **반정규화**: 정규화된 테이블을 성능 향상을 위해 의도적으로 중복을 허용하는 과정
- **반정규화 기법**
  - **테이블 병합**: 1:1 관계 또는 1:N 관계의 테이블을 합침
  - **테이블 분할**: 수직 분할(컬럼 기준), 수평 분할(행 기준)
  - **중복 테이블 추가**: 집계 테이블, 진행 테이블, 특정 부분만 포함하는 테이블
  - **컬럼 중복**: 자주 사용하는 컬럼을 중복 저장하여 JOIN 횟수 감소
- **반정규화 적용 시기**: 조회 성능이 중요한 경우, 대량 데이터 처리, 조인이 빈번한 경우`
    },
    {
      id: 'ch4',
      name: '트랜잭션',
      topics: ['ACID 특성', '동시성 제어', '회복 기법', '교착상태'],
      content: `## 트랜잭션

### 트랜잭션 개요
- **트랜잭션(Transaction)**: 데이터베이스의 상태를 변환시키는 하나의 논리적 작업 단위
- 하나의 트랜잭션은 모두 완료(Commit)되거나 모두 취소(Rollback)되어야 함

### ACID 특성
- **원자성(Atomicity)**: 트랜잭션은 모두 실행되거나 모두 취소되어야 함 (All or Nothing)
- **일관성(Consistency)**: 트랜잭션 완료 후 데이터베이스는 일관된 상태를 유지
- **독립성/격리성(Isolation)**: 트랜잭션 실행 중 다른 트랜잭션이 간섭할 수 없음
- **지속성/영속성(Durability)**: 완료된 트랜잭션의 결과는 영구적으로 반영

### 트랜잭션 상태
- **활동(Active)**: 트랜잭션이 실행 중인 상태
- **부분 완료(Partially Committed)**: 마지막 연산까지 실행했지만 Commit 전
- **완료(Committed)**: 트랜잭션이 성공적으로 완료되어 Commit 실행
- **실패(Failed)**: 오류가 발생하여 중단된 상태
- **철회(Aborted)**: 트랜잭션이 취소되고 Rollback 수행된 상태

### 동시성 제어 (Concurrency Control)
- **동시성 제어**: 여러 트랜잭션이 동시에 실행될 때 데이터의 일관성을 유지하기 위한 기법
- **동시성 문제**
  - **갱신 분실(Lost Update)**: 동시에 같은 데이터를 수정하여 하나의 수정이 무효화
  - **비완료 의존(Uncommitted Dependency)**: 다른 트랜잭션의 미완료 데이터를 읽음 (Dirty Read)
  - **모순(Inconsistency)**: 동시 실행 결과가 직렬 실행 결과와 다름
  - **연쇄 복귀(Cascading Rollback)**: 한 트랜잭션의 실패가 연쇄적인 복귀를 유발

### 로킹 (Locking)
- **로킹**: 트랜잭션이 데이터에 접근하기 전에 잠금(Lock)을 설정
- **공유 잠금(Shared Lock, S-Lock)**: 읽기만 가능, 여러 트랜잭션이 동시에 설정 가능
- **배타 잠금(Exclusive Lock, X-Lock)**: 읽기/쓰기 모두 가능, 하나의 트랜잭션만 설정
- **2단계 로킹 프로토콜(2PL)**: 확장 단계(Lock만 가능)와 축소 단계(Unlock만 가능)로 구분
  - 직렬 가능성을 보장하지만 교착상태 발생 가능
- **로킹 단위**: 데이터베이스 > 테이블 > 페이지 > 행(레코드)
  - 로킹 단위가 크면: 병행성 감소, 관리 용이
  - 로킹 단위가 작으면: 병행성 증가, 관리 복잡

### 타임스탬프 기법
- **타임스탬프(Timestamp)**: 트랜잭션에 고유 시간 표시를 부여하여 순서를 결정
- 직렬 가능성을 보장, 교착상태가 발생하지 않음

### 회복 기법 (Recovery)
- **로그 기반 회복**
  - **지연 갱신(Deferred Update)**: 트랜잭션 완료 전까지 실제 DB에 반영하지 않음 (Redo만 필요)
  - **즉시 갱신(Immediate Update)**: 트랜잭션 실행 중 바로 DB에 반영 (Redo와 Undo 모두 필요)
- **체크포인트(Checkpoint) 회복**: 특정 시점에 검사점을 설정하여 회복 범위를 축소
- **그림자 페이징(Shadow Paging)**: 변경 전 페이지를 그림자로 보관, 로그 불필요
- **Redo(재실행)**: 완료된 트랜잭션의 결과를 다시 반영
- **Undo(취소)**: 미완료 트랜잭션의 변경 사항을 원래 상태로 복구

### 교착상태 (Deadlock)
- **교착상태**: 두 개 이상의 트랜잭션이 서로의 잠금 해제를 기다리며 무한 대기하는 상태
- **예방(Prevention)**: 교착상태 발생 조건을 사전에 차단
- **탐지(Detection)**: 대기 그래프(Wait-for Graph)를 사용하여 교착상태 탐지
- **회복**: 교착상태에 관련된 트랜잭션 중 하나를 강제 종료(Rollback)`
    }
  ],
  'info-comm': [
    {
      id: 'ch1',
      name: '네트워크',
      topics: ['LAN/WAN/MAN', '토폴로지', '프로토콜', '스위칭', '라우팅'],
      content: `## 네트워크

### 네트워크 분류
- **LAN(Local Area Network)**: 근거리 통신망, 한 건물이나 캠퍼스 내 네트워크
  - 이더넷(Ethernet), 토큰 링(Token Ring), FDDI
  - 전송 속도 빠름, 오류 발생률 낮음
- **MAN(Metropolitan Area Network)**: 도시 규모 통신망, LAN과 WAN의 중간 규모
  - DQDB(Distributed Queue Dual Bus) 표준
- **WAN(Wide Area Network)**: 광역 통신망, 국가 또는 대륙 간 연결
  - 전용 회선, 교환 회선(회선 교환, 패킷 교환) 사용

### 토폴로지 (Topology)
- **성형(Star)**: 중앙 허브를 중심으로 연결, 장애 격리 용이, 중앙 장비 고장 시 전체 마비
- **버스형(Bus)**: 하나의 공유 통신선에 모든 노드 연결, 설치 간단, 충돌(Collision) 발생 가능
- **링형(Ring)**: 원형으로 연결, 토큰 패싱 방식, 한 노드 고장 시 전체 영향
- **망형(Mesh)**: 모든 노드가 서로 연결, 신뢰성 최고, 비용이 많이 듦
  - 회선 수: n(n-1)/2 (완전 연결 시)
- **트리형(Tree)**: 계층적 구조, 버스형의 변형, 확장 용이

### 프로토콜 (Protocol)
- **프로토콜**: 통신 규약, 데이터 교환을 위한 약속
- **프로토콜 구성 요소**: **구문(Syntax)**, **의미(Semantics)**, **타이밍(Timing)**
- **주요 프로토콜**
  - **TCP**: 연결 지향, 신뢰성, 흐름 제어, 혼잡 제어
  - **UDP**: 비연결, 빠른 전송, 실시간 서비스
  - **IP**: 비연결형, 데이터그램 방식, 비신뢰성
  - **ARP**: IP 주소 → MAC 주소 변환
  - **RARP**: MAC 주소 → IP 주소 변환
  - **ICMP**: 오류 보고 및 진단 (ping 명령어)
  - **DHCP**: IP 주소 자동 할당

### 스위칭 (Switching)
- **회선 교환(Circuit Switching)**: 통신 경로를 미리 설정, 전용 회선 사용, 실시간 통신에 적합
- **패킷 교환(Packet Switching)**: 데이터를 패킷으로 분할하여 전송
  - **데이터그램 방식**: 패킷마다 독립적으로 경로 설정, 도착 순서 다를 수 있음
  - **가상 회선 방식**: 먼저 논리적 경로를 설정한 후 패킷 전송, 순서 보장
- **메시지 교환(Message Switching)**: 전체 메시지를 축적-전달(Store-and-Forward)

### 라우팅 (Routing)
- **라우팅**: 패킷이 목적지까지 도달하기 위한 최적 경로를 선택하는 과정
- **정적 라우팅(Static)**: 관리자가 수동으로 경로 설정
- **동적 라우팅(Dynamic)**: 라우팅 프로토콜에 의해 자동으로 경로 갱신
- **라우팅 프로토콜**
  - **RIP**: 거리 벡터(Distance Vector) 방식, 홉(Hop) 수 기준, 최대 15홉
  - **OSPF**: 링크 상태(Link State) 방식, 대규모 네트워크에 적합, 다익스트라 알고리즘 사용
  - **BGP**: 경계 게이트웨이 프로토콜, 자율 시스템(AS) 간 라우팅
- **CSMA/CD**: 이더넷(유선 LAN)의 매체 접근 제어, 충돌 감지 시 재전송
- **CSMA/CA**: 무선 LAN의 매체 접근 제어, 충돌 회피`
    },
    {
      id: 'ch2',
      name: '인터넷',
      topics: ['IP 주소', 'DNS', 'HTTP/HTTPS', '이메일 프로토콜', '웹 기술'],
      content: `## 인터넷

### IP 주소
- **IPv4**: 32비트 주소, 8비트씩 4부분으로 구분 (예: 192.168.1.1)
- **IP 주소 클래스**
  - **A 클래스**: 0.0.0.0 ~ 127.255.255.255 (대규모 네트워크, 네트워크 8비트 + 호스트 24비트)
  - **B 클래스**: 128.0.0.0 ~ 191.255.255.255 (중규모, 네트워크 16비트 + 호스트 16비트)
  - **C 클래스**: 192.0.0.0 ~ 223.255.255.255 (소규모, 네트워크 24비트 + 호스트 8비트)
  - **D 클래스**: 224 ~ 239 (멀티캐스트용)
  - **E 클래스**: 240 ~ 255 (연구용/예약)
- **사설 IP 대역**: 10.x.x.x, 172.16~31.x.x, 192.168.x.x
- **서브네팅(Subnetting)**: 네트워크를 더 작은 단위로 분할, 서브넷 마스크 사용
  - **서브넷 마스크**: 네트워크 부분과 호스트 부분을 구분 (예: 255.255.255.0)
  - 호스트 수 = 2^(호스트 비트 수) - 2 (네트워크 주소, 브로드캐스트 주소 제외)
- **IPv6**: 128비트 주소, 16비트씩 8부분(콜론 구분), 주소 부족 문제 해결

### DNS (Domain Name System)
- **DNS**: 도메인 이름을 IP 주소로 변환하는 시스템
- **도메인 구조**: 루트 → 최상위 도메인(com, org, kr) → 2차 도메인 → ...
- **DNS 동작**: 재귀적 질의(Recursive Query), 반복적 질의(Iterative Query)
- **DNS 레코드**: A(IPv4), AAAA(IPv6), CNAME(별칭), MX(메일), NS(네임서버)

### HTTP/HTTPS
- **HTTP(HyperText Transfer Protocol)**: 웹에서 데이터를 주고받는 프로토콜, 포트 80
- **HTTP 메서드**: GET(조회), POST(생성), PUT(수정), DELETE(삭제), PATCH(부분 수정)
- **HTTP 상태 코드**
  - **2xx**: 성공 (200 OK, 201 Created)
  - **3xx**: 리다이렉션 (301 Moved Permanently, 304 Not Modified)
  - **4xx**: 클라이언트 오류 (400 Bad Request, 403 Forbidden, 404 Not Found)
  - **5xx**: 서버 오류 (500 Internal Server Error, 503 Service Unavailable)
- **HTTPS**: HTTP + SSL/TLS 암호화, 포트 443, 보안 통신

### 이메일 프로토콜
- **SMTP(Simple Mail Transfer Protocol)**: 메일 전송 프로토콜, 포트 25
- **POP3(Post Office Protocol 3)**: 메일 수신, 서버에서 다운로드 후 삭제, 포트 110
- **IMAP(Internet Message Access Protocol)**: 메일 수신, 서버에 메일 유지, 여러 기기 동기화, 포트 143

### 웹 기술
- **HTML**: 웹 페이지의 구조와 내용을 정의하는 마크업 언어
- **CSS**: 웹 페이지의 스타일(디자인)을 정의하는 스타일시트 언어
- **JavaScript**: 웹 페이지에 동적 기능을 구현하는 프로그래밍 언어
- **쿠키(Cookie)**: 클라이언트 측에 저장되는 작은 데이터, 상태 유지
- **세션(Session)**: 서버 측에서 관리하는 사용자 상태 정보
- **URL 구조**: 프로토콜://도메인:포트/경로?쿼리#프래그먼트
- **REST(Representational State Transfer)**: 자원 중심의 웹 서비스 설계 방식`
    },
    {
      id: 'ch3',
      name: '정보 보안',
      topics: ['암호화', '해시', '전자서명', 'SSL/TLS', '공격유형', '방화벽'],
      content: `## 정보 보안

### 정보보안 3요소 (CIA)
- **기밀성(Confidentiality)**: 인가된 사용자만 정보에 접근 가능
- **무결성(Integrity)**: 정보가 인가 없이 변경되지 않음을 보장
- **가용성(Availability)**: 인가된 사용자가 필요할 때 정보에 접근 가능

### 암호화 기법
- **대칭키(비밀키) 암호화**: 암호화와 복호화에 같은 키 사용
  - **DES**: 64비트 블록, 56비트 키, 16라운드 Feistel 구조
  - **AES**: 128/192/256비트 키, DES의 대체 표준
  - **ARIA**: 한국 표준 블록 암호
  - **SEED**: 한국 개발 128비트 블록 암호
  - 장점: 속도 빠름 / 단점: 키 분배 문제
- **비대칭키(공개키) 암호화**: 공개키로 암호화, 개인키로 복호화 (또는 그 반대)
  - **RSA**: 큰 소수의 곱을 인수분해하기 어려운 점 이용
  - **ECC**: 타원 곡선 이용, RSA보다 짧은 키로 동일 보안 수준
  - 장점: 키 분배 용이 / 단점: 속도 느림

### 해시 함수
- **해시**: 임의 길이의 입력을 고정 길이 출력으로 변환하는 일방향 함수
- **특징**: 역변환 불가(일방향), 같은 입력은 항상 같은 출력, 충돌 저항성
- **알고리즘**: MD5(128비트), SHA-1(160비트), **SHA-256**(256비트)
- **활용**: 비밀번호 저장, 데이터 무결성 검증, 전자서명

### 전자서명 (Digital Signature)
- **전자서명**: 송신자의 개인키로 서명, 수신자가 공개키로 검증
- **기능**: 인증(Authentication), 무결성(Integrity), 부인 방지(Non-repudiation)
- **과정**: 메시지의 해시값을 개인키로 암호화하여 서명 생성

### SSL/TLS
- **SSL(Secure Socket Layer) / TLS(Transport Layer Security)**: 전송 계층 보안 프로토콜
- 웹 브라우저와 서버 간 데이터를 암호화하여 안전하게 전송
- **인증서(Certificate)**: 서버의 공개키와 신원 정보를 포함, CA(인증기관)가 발급
- **핸드셰이크 과정**: 프로토콜 버전 협상 → 암호 스위트 선택 → 인증서 교환 → 세션키 생성

### 주요 공격 유형
- **DoS(Denial of Service)**: 서비스 거부 공격, 서버에 과도한 트래픽 발생
  - **DDoS**: 분산 서비스 거부 공격, 여러 좀비 PC를 이용
  - **SYN Flooding**: TCP 3-way handshake의 취약점을 이용한 공격
- **SQL 인젝션(SQL Injection)**: 입력값에 악의적인 SQL문을 삽입하여 DB 조작
  - 대응: 입력값 검증, PreparedStatement 사용, 최소 권한 원칙
- **XSS(Cross-Site Scripting)**: 웹 페이지에 악성 스크립트를 삽입하여 사용자 정보 탈취
  - 대응: 입출력 값 검증, HTML 인코딩
- **CSRF(Cross-Site Request Forgery)**: 사용자의 인증 정보를 이용해 의도하지 않은 요청 실행
- **피싱(Phishing)**: 신뢰할 수 있는 기관을 사칭하여 개인정보 탈취
- **스니핑(Sniffing)**: 네트워크 패킷을 감청하여 정보 탈취
- **스푸핑(Spoofing)**: 신원을 위조하여 접근 (IP, ARP, DNS 스푸핑)

### 방화벽 및 보안 장비
- **방화벽(Firewall)**: 내부 네트워크와 외부 네트워크 간의 접근을 제어
  - 패킷 필터링, 상태 검사(Stateful Inspection), 응용 게이트웨이
- **IDS(침입 탐지 시스템)**: 네트워크/시스템의 비정상 활동을 탐지
- **IPS(침입 방지 시스템)**: 탐지 + 차단까지 수행
- **VPN(Virtual Private Network)**: 공중 네트워크를 통해 안전한 사설 네트워크 구현`
    },
    {
      id: 'ch4',
      name: '신기술 동향',
      topics: ['클라우드', '빅데이터', 'IoT', 'AI/머신러닝', '블록체인', '메타버스', '디지털 트윈'],
      content: `## 신기술 동향

### 클라우드 컴퓨팅 (Cloud Computing)
- **클라우드**: 인터넷을 통해 IT 자원을 필요한 만큼 빌려서 사용하는 서비스
- **서비스 모델**
  - **IaaS(Infrastructure as a Service)**: 서버, 스토리지, 네트워크 등 인프라 제공 (예: AWS EC2)
  - **PaaS(Platform as a Service)**: 개발 플랫폼 제공 (예: Google App Engine, Heroku)
  - **SaaS(Software as a Service)**: 완성된 소프트웨어 제공 (예: Gmail, MS 365, Dropbox)
- **배포 모델**: 퍼블릭 클라우드, 프라이빗 클라우드, 하이브리드 클라우드, 커뮤니티 클라우드
- **가상화(Virtualization)**: 물리적 자원을 논리적으로 분할하여 사용
- **컨테이너(Container)**: 경량 가상화 기술 (예: Docker), 운영체제 수준의 격리

### 빅데이터 (Big Data)
- **빅데이터 3V**: Volume(대량), Velocity(빠른 속도), Variety(다양성)
- **추가 특성**: Value(가치), Veracity(정확성)
- **빅데이터 처리 기술**
  - **하둡(Hadoop)**: 분산 파일 시스템(HDFS)과 MapReduce 기반 분산 처리 프레임워크
  - **MapReduce**: 대용량 데이터를 Map(분할)과 Reduce(결합)으로 병렬 처리
  - **스파크(Spark)**: 인메모리 기반 빅데이터 처리, Hadoop보다 빠른 처리 속도
- **데이터 마이닝(Data Mining)**: 대량의 데이터에서 유용한 패턴이나 지식을 발견하는 기법
  - 분류(Classification), 군집화(Clustering), 연관 규칙(Association Rule), 예측(Prediction)

### IoT (Internet of Things, 사물인터넷)
- **IoT**: 사물에 센서와 통신 기능을 내장하여 인터넷에 연결하는 기술
- **구성 요소**: 센서/디바이스, 네트워크(통신), 플랫폼, 서비스/응용
- **통신 기술**: Bluetooth, Zigbee, LoRa, NB-IoT, Wi-Fi, 5G
- **활용 분야**: 스마트홈, 스마트시티, 스마트팩토리, 헬스케어, 자율주행
- **엣지 컴퓨팅(Edge Computing)**: 데이터 발생 지점(엣지) 근처에서 처리, 지연시간 감소

### AI / 머신러닝 (Artificial Intelligence / Machine Learning)
- **인공지능(AI)**: 인간의 학습, 추론, 판단 능력을 모방한 컴퓨터 기술
- **머신러닝(Machine Learning)**: 데이터로부터 패턴을 학습하여 예측/판단
  - **지도 학습(Supervised)**: 입력과 정답(레이블)을 함께 학습 (분류, 회귀)
  - **비지도 학습(Unsupervised)**: 레이블 없이 데이터의 구조/패턴을 학습 (군집화, 차원 축소)
  - **강화 학습(Reinforcement)**: 보상을 최대화하는 행동을 학습
- **딥러닝(Deep Learning)**: 인공신경망(ANN)을 다층으로 구성, 복잡한 패턴 학습
  - CNN(이미지), RNN/LSTM(순차 데이터), GAN(생성 모델)
- **자연어 처리(NLP)**: 인간의 언어를 컴퓨터가 이해하고 처리

### 블록체인 (Blockchain)
- **블록체인**: 분산 원장(Distributed Ledger) 기술, 데이터를 블록 단위로 체인처럼 연결
- **특징**: 탈중앙화, 투명성, 불변성(위변조 방지), 합의 알고리즘
- **합의 알고리즘**: PoW(작업 증명), PoS(지분 증명), DPoS(위임 지분 증명)
- **활용**: 암호화폐(비트코인, 이더리움), 스마트 계약, 공급망 관리, 디지털 신원 인증
- **스마트 계약(Smart Contract)**: 조건이 충족되면 자동으로 실행되는 프로그래밍된 계약

### 메타버스 (Metaverse)
- **메타버스**: 현실과 가상이 융합된 3차원 가상 세계
- **유형**: 증강현실(AR), 가상현실(VR), 라이프로깅, 거울세계
- **핵심 기술**: VR/AR/MR(혼합현실), 아바타, 디지털 트윈, 5G/6G 통신

### 디지털 트윈 (Digital Twin)
- **디지털 트윈**: 물리적 객체를 가상 공간에 동일하게 복제한 디지털 모델
- **특징**: 실시간 데이터 반영, 시뮬레이션, 예측 분석
- **활용**: 제조업(공장 시뮬레이션), 도시 계획(스마트시티), 의료(가상 환자 모델), 건설

### 기타 신기술
- **DevOps**: 개발(Development)과 운영(Operations)의 통합, CI/CD 자동화
- **마이크로서비스 아키텍처(MSA)**: 서비스를 작은 단위로 분리하여 독립 배포
- **서버리스(Serverless)**: 서버 관리 없이 코드 실행 (예: AWS Lambda)
- **제로 트러스트(Zero Trust)**: "아무도 신뢰하지 않는다"는 보안 모델, 모든 접근 검증`
    }
  ]
};
